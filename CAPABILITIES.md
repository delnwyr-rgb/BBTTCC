# Bad Eden / BBTTCC — Capability Atlas

**Generated 2026-08-27** from a full six-crew code survey (system core + all 24 modules).
This is the canonical "what exists and what can it do" inventory. File:line pointers are
as of the survey date — treat them as landing zones, not gospel, after heavy edits.

**MAINTENANCE RULE:** when a working session adds, renames, or removes a public API,
hook, setting, flag schema, or tool macro, update that module's section here in the
same session (same commit when committing). New modules get a new section. The
**Dormant / Broken registry** (Appendix A) is the "what we're both forgetting" list —
retire entries when fixed, add entries when you build something and don't wire it yet.

Legend: 🔴 broken wire · 🟡 dormant/unwired (built, nothing calls it) · ⚠ trap/gotcha

---

## Contents

- [0. Cross-cutting contracts](#0-cross-cutting-contracts)
- [1. systems/fourththing — the system core](#1-systemsfourththing)
- [2. bbttcc-core](#2-bbttcc-core) · [3. bbttcc-factions](#3-bbttcc-factions) · [4. bbttcc-banks](#4-bbttcc-banks) · [5. bbttcc-territory](#5-bbttcc-territory) · [6. bbttcc-world](#6-bbttcc-world) · [7. bbttcc-structures](#7-bbttcc-structures) · [8. bbttcc-facility-console](#8-bbttcc-facility-console)
- [9. bbttcc-campaign](#9-bbttcc-campaign) · [10. bbttcc-travel](#10-bbttcc-travel) · [11. bbttcc-encounters](#11-bbttcc-encounters)
- [12. bbttcc-raid](#12-bbttcc-raid) (incl. Courtly, Tableau, Siege, Agent API)
- [13. bbttcc-mal-voice](#13-bbttcc-mal-voice) (AI NPC engine) · [14. bbttcc-market](#14-bbttcc-market) · [15. bbttcc-onboarding](#15-bbttcc-onboarding) · [16. bbttcc-master-content](#16-bbttcc-master-content) · [17. bbttcc-sorting-engine](#17-bbttcc-sorting-engine)
- [18. bbttcc-aae](#18-bbttcc-aae) · [19. bbttcc-auto-link](#19-bbttcc-auto-link) · [20. bbttcc-character-options](#20-bbttcc-character-options) · [21. bbttcc-epic](#21-bbttcc-epic) · [22. bbttcc-fx-integration](#22-bbttcc-fx-integration) · [23. bbttcc-radiation](#23-bbttcc-radiation) · [24. bbttcc-tikkun](#24-bbttcc-tikkun)
- [25. surge-powers (Kaeron War world — NOT Bad Eden)](#25-surge-powers)
- [Appendix A — Dormant / Broken registry](#appendix-a--dormant--broken-registry)
- [Appendix B — Hook census highlights](#appendix-b--hook-census-highlights)

---

## 0. Cross-cutting contracts

**API namespaces**
- `game.bbttcc.api.*` — module APIs (core bootstraps null slots so load order can't lose one).
- `game.fourththing.*` (alias `game.rollForInitiation`) — system core. `game.fourththing.epic` = bbttcc-epic (deliberate exception: RFI-exclusive endgame).
- `game.bbttcc.mal.*` — mal-voice AI layer. `game.bbttcc.help` — central tooltip/tour registry (adoption is partial, see modules). `game.bbttcc.apps.*` — app class/instance registry.
- `game.surgePowers` — surge-powers, **dnd5e Kaeron War world only**, no BBTTCC ties.

**Hook naming** — system emits dot-names (`fourththing.manifestationCast`); modules emit `bbttcc:ns:event`. Known deviations: `fourththing:itemAnimated` (deliberate colon holdout), `bbttcc.mutationRoll` (radiation, dot), `bbttcc:enlightenmentChanged` / `bbttcc:opsRecalculated` / `bbttcc:hexImprovement` / `bbttcc:advanceTurn:begin|end` (flat/camel, no ns segment).

**OP economy** — nine channels `violence, nonlethal, intrigue, economy, softpower, diplomacy, logistics, culture, faith` (canon: `bbttcc-factions/scripts/op-engine.js:17-27`). **All storage in MARKS, 1 OP = 10 marks** (`OP_TO_MARKS`, `op-engine.js:14`). Tier-band caps in marks `[50,70,90,110,130]` (`op-engine.js:116`). Banks live at `flags["bbttcc-factions"].opBank.<channel>`. ⚠ The channel list and the ×10 ratio are hardcoded in ~7 and ~5 extra places respectively (`bbttcc-bridge.js:437,449,513`, `exchange-engine.js:281`, `faction-bank-app.js:26`, vault-core `OP_BUCKETS`, diplomacy-apps, unity-bonus, make-salvage macro) — read from `api.op.KEYS`/`OP_TO_MARKS` in new code.

🪤 **Save restore demoted every town to an outpost (fixed 2026-09-12):** `api.world.saves` restore recreates every hex drawing, and `overview-button.js` "pristine wilderness defaults" on `preCreateDrawing`/`createDrawing` used to REWRITE legacy-looking values (type settlement → wilderness, size town → outpost, population medium → uninhabited) — so each restore silently re-sized the whole map with no ledger entry. Now fill-only, and skipped entirely for any drawing that already carries a territory flag block. `effects-wilderness establish_outpost` also fixed: `f.size || "outpost"` kept size "none" (truthy string) → every new outpost read None; and founding now zeroes `mods.loyalty/morale` (settlers bring their own mood — inherited unrest history no longer rolls into the first unrest check). **Phase door + tier floor before regen (owner ruling 2026-09-12, sim'd first):** `driverAdvanceTurn` awaits `api.campaign.director.openCalendarDoors({turn: cur+1})` and then `reconcileLevels()` before `advanceOPRegen` — the calendar hard doors (`PHASE_CALENDAR_DOORS`) open against the turn the Advance PRODUCES, the act's tier floor raises opCaps, and only then does income land. The old end-of-turn door (kept as a safety net in the ledger listener) let turn 1 of an act clamp every full channel's income against the old tier's caps. Side effect: the ACT banner + "wants a word" invitations now post before that turn's digest, not after. `bin/ft-sim-economy --door-order pre|post` models both orders (turn-1 clamp loss 37–59 marks → 0–1). **Regen before spend (owner ruling 2026-09-12):** `driverAdvanceTurn` order is now promote → base delegate → scheduled OP → `advanceOPRegen` → `plannedRaidsStep` (activities paid from the bank AFTER income lands) → `tickFactionBonuses` → logistics → queued effects. The dry preview checks affordability against bank + `computeTerritoryMatrixIncome`. **THE ECONOMY CONSTANTS TABLE — `bbttcc-core/scripts/economy.constants.js` (item 5, 2026-09-12):** every number the economy tunes lives here and nowhere else — `TIER_CAP_BAND_MARKS`, `LOGISTICS_CAPACITY_FLOOR_MARKS`, `PRICE_MULT`, `RECIPES`, `LEDGER_DAY_COST`, `TYPE_BASE` (with a `wilderness` row since 2026-09-12 — 1/1/1 base, so a wild outpost yields 1/1/1 instead of falling through to the settlement ladder's 1/1/2; `manualOverride` hexes keep their hand-set vector), `MOD_RESOURCE` (per-RESOURCE multipliers, 2026-09-13: `"fallout bloom": {food:-0.5}` — `getModifierEffects` returns `mRes`, `computeEffectiveResources` applies it per resource and stores `multipliers.mRes` only when one is in play), `LANE_HEX_BONUS.<lane>.byModifier` (`faith: {"red thread field": 1}`, doubled on leyline `surge` via `LANE_BY_MODIFIER_SURGE_MULT`), `MOD_PRODUCTION["red thread field"] = 0.10`, `SIZE_MULT`, `MOD_PRODUCTION`/`MOD_TRADE`, `HEX_MOD_LOYALTY`, `RES_TO_OP` (8 lanes), `LEY_FLOW_MULT`, `LANE_HEX_BONUS`, `LOGI`, `UPKEEP{…}`, `TERRAIN_TABLE`. Owners ES-import it (op-engine re-exports the band for its facts; turn-driver; territory main.js; garrison upkeep; strategic-throughput; hex-travel) — bbttcc-territory / -raid / -factions / -travel now DECLARE `requires: bbttcc-core` — 🪤 a file that `import`s MUST be listed under `esmodules`, not `scripts` (a classic script throws "Cannot use import statement outside a module" and silently never loads: garrison upkeep, the tier button and hex-travel were moved 2026-09-12 after exactly that). 🪤 module esmodules evaluate in FETCH order across modules, so `game.bbttcc.lifecycle` may not exist at another module's script load — look it up at `ready` (the route enhancers do, and log "loaded"/"installed"). 🪤 Neither Lightsail instance has a default world: every `pm2 restart` parks Foundry at the setup screen until the owner launches the world (⚠ module.json edits → pm2 restart + FULL client cache bust). Gear / creature / the OP↔marks ratio stay in the SYSTEM's `rfi-pricing.js`. `bin/ft-sim-economy` parses this one file; `bin/ft-lint-facts` F6 fails any other file that defines one of these tables (`facts-ok` tags a same-named non-economy table, e.g. the VFX `RECIPES`). **ONE FOUNDING PIPELINE (item 6):** `incarnation-forge.js foundPlayerFaction` now calls `api.factions.applyStartingPackage({packageKey:"standard", starterRig:"hexmobile"})` right after `Actor.create` — onboarding factions are born exactly as wizard factions are (caps, bank seed, tracks 25/25, stockpile, doctrine, starter rig); the hand-seeded parity flags are gone; `repair-onboarded-faction-parity` stays for pre-2026-09-12 factions. **LIFECYCLE CONTRACT — `game.bbttcc.lifecycle` (item 4, 2026-09-12):** `provide(cap, value)` when a capability is final for the session, `rebuilt(cap)` whenever it is re-normalized/replaced, `need(cap) → Promise` once, `onRebuild(cap, fn)` to re-apply an extension; installed at bbttcc-core script load (order-independent). First capability: `raid.EFFECTS` — compat-bridge `publishCompat` provides it and `_normalizeEffectsRegistry` fires `rebuilt`; the trade-route + supply-line enhancers and strategic-throughput's price policy / CAN_PLAN attach through it (their retry timers are gone; the old `whenRaidReady` poll remains only as the no-core fallback). Migration path for the other ~29 `whenRaidReady` enhancers: same three lines. **RESTORE MODE:** `api.save load()` sets `game.bbttcc.restoring`, passes `{bbttccRestore:true}` to every `createEmbeddedDocuments`, and fires `bbttcc:restore:begin/end`; territory's pristine-hex defaults and raid's post→turn normalizer stand down during it — a restored document is not a new one. 🪤 **Beat effects applied at chain SETTLE (fixed 2026-09-13):** `executeBeat` used to apply world mutation + quest effects at its tail — after `_runBeatDialog` returned, i.e. after the chosen next beat and everything beneath it. Repeatable town hubs only return on the final exit, so the whole chain unwound at once, deepest first: a closing beat's "complete" landed, then its parent acceptance beat's "accept" flipped the finished quest back to active (three Lyrenn quests, Post Lyrenn Act 2 V1). Now `_applyBeatEffectsOnce` applies them at the reply pick (every routing site) with the tail as fallback, memo on ctx (`__beatFxApplied`; `_chainCtxFrom` builds a fresh ctx so a re-entered beat applies again). Same disease/cure as phaseAdvance-at-entry and Word-answered-at-pick. Authoring rule: a "Word from <town>" SUMMONS beat must gate on the quest it summons for (`questBucket isNot active/completed`) — `tools/patch-lyrenn-word-gates.macro.js` adds them to Treeline/East Channels and repairs the tracks. **Beats can now write the yield engine's modifier list (2026-09-13):** `worldEffects.hexModifiers = {add:[…], remove:[…], hexName?|targetHexUuid?}` (or a LIST of such blocks) — applied immediately by world-mutation-engine to `flags.bbttcc-territory.modifiers` + `recomputeHexResources` + hex ledger + GM whisper (🪤 the older `worldEffects.worldModifiers` → `tf.worldModifiers` is DISPLAY-ONLY: the hex sheet reads it, nothing in the economy does). `worldEffects.hexReading = {hexName, voice?}` = the Water Choir as an instrument: a player-facing card in bands (the tenders' mood = coalition morale/loyalty; the hurt = darkness + Fallout Bloom + count of blast-marked coalition hexes; the lean = leyline flow/purity + Red Thread) with a GM raw-facts whisper. `PHASE_DOOR_BEATS = {2:"a2_that_one_night"}` — `_storyPhaseAdvance` fires the act's door beat (fire-and-forget) before the invite scan, so "That One Night" lands ON the Advance. Seeder: `tools/seed-lyrenn-blast-treatment.macro.js` (DRY_RUN) — spec `~/LYRENN_ACT2_REFRAME_2026_09_13.md`. **HEX STATES — one catalog, two surfaces (2026-09-13):** `HEX_MODIFIER_CATALOG` (generic states) + `WORLD_MODIFIERS` (unique states: harmonized_grove, fallout_bloom, red_thread_field) live in the economy constants, published at `game.bbttcc.facts.hexStates`; the Hex Config grid renders both (collapsing `auto-fill` grid — the old twelve hand-typed labels overlapped) and the Beat Editor's World Effects panel renders both as rows (— / Add / Remove, duration, enabled, target hex from EVERY scene). A World Modifier row = the chip on the hex sheet + its named states in `tf.modifiers` (`row.modifiers`, or the registry's) — `world-mutation-engine._applyNamedModifiers` writes both, recomputes, ledgers, whispers; `op:"remove"` lifts both; rows with a duration EXPIRE at `bbttcc:advanceTurn:end` (`sweepExpiredWorldModifiers`, also on `api.worldMutation`). Harmonized Grove is now real: +5% production, +1 loyalty (the forest/diplomacy channels are retired promises). `worldEffects.hexModifiers` stays as the seeder/API primitive. **STORY MODEL — Phase 1 of the Director rebuild (2026-09-13, `scripts/story-model.js`):** `game.bbttcc.api.campaign.story.{QUEST_MAP, registryOf, deriveSituation}`. Vocabulary = quest · chapter · ending: `QUEST_MAP` folds the registry's 68 rows into 28 quests (16 `word_*` tickets and the travel pool excluded; former sub-quests become chapters; `keystone` flag; `hex`); `deriveSituation(ctx)` derives `{act, turn, quests[{state dormant|offered|active|completed|closed, chapters[{state dormant|open|done, ending, next}], next, why, currentChapter}], anchor, now{quest, chapter, next, why, roads}, inPlay[], doors[]}` from the beats' own `questEffects` start/complete rows, the coalition quest buckets and the fired ledgers (reads only). One rule replaces the Visualizer's twelve-rung hero ladder: **a gate on quest X resolves to X's own next**. The Visualizer's NOW card is now a RENDER of `story.now` (`_heroFromStory`, ladder kept as fallback); `_computeSituation` returns `story`. **Phase 2 (2026-09-13, shipped):** beats declare `beat.story = {quest, chapter?, role: start|ending|closer, ending?, alsoStarts?[], alsoEnds?[]}` (migration `tools/migrate-story-declarations.macro.js`, DRY_RUN; `declarationsFor(beats)` derives them from questEffects rows + questRole; hand-authored blocks marked `__hand:true` are kept). THE ONE STORE = world setting `bbttcc-campaign.storyState[campaignId] = {played, started, chapters{q:{ch:{started, ending}}}, closed}`; ONE writer `_storyRecord` at `executeBeat` ENTRY (`applyRecord`, pure) → `_storyProject` rewrites the coalition quest buckets as a PROJECTION (`projection`; never un-starts, never touches archived) → hook `bbttcc:story:changed` + GM "Story" whisper. `worldEffects.questEffects` accept/complete rows on a DECLARED beat no longer move buckets (record-keeping only). Seals for declared beats come from the store (`sealOfDecl`): keystones never seal, a started quest stays open across acts, a never-started quest closes with its act; undeclared beats keep the old bucket/act logic. `api.campaign.story.{state, record, project, bootstrap, reset}`; `bootstrap` replays the fired ledgers + buckets into the store (idempotent). **Phase 3 (2026-09-13, shipped) — THE SLATE:** the Director no longer picks beats on pressure. `directorSlate({via})` runs at every applied `bbttcc:advanceTurn:end` (250 ms after the settle whispers) and on `api.campaign.director.slate()`: one GM card — NOW (the story's quest/chapter → next, ▶ if ready, else what it waits for via `roadsFor`), IN PLAY (started quests, their next, where), DOORS (every quest of the act whose start is ready, ✉ if an invitation card is out), CALENDAR (next hard door in N turns, or sooner when the act's keystone closes). ▶ buttons (`data-bbttcc-slate-run`, GM only) call `runBeat`; hook `bbttcc:director:slate`. `_storySituation(campaign, {full})` = two-pass derivation (structural, then real `_beatRequiresMet` on each quest's candidates only). **Invitations** (`_directorIssueInvites`): quest/chapter START beats with an invite line + speaker, ready, not invited, not played → one card each via `_postTalkInvitation`; issued at the slate and after every resolved beat (`_onBeatResolvedInviteScan` now does only this — the speaker-wide offer scan that flooded ten cards is gone). **Retired:** the travel/raid/beat pressure listeners and `_directorSeamLook` (no pressure accrues anywhere; `DIRECTOR_PRESSURE`, `director.pressureThreshold`, `director.turnTickOnlyChains` inert); `directorTick` stays only at `api.campaign.director.tick` for a deliberate manual pick. **Phase 4 (2026-09-14, shipped):** the fired ledgers are VIEWS — nothing writes `firedStoryBeats`/`dialogueFired` any more (executeBeat entry mark, `_onBeatResolvedStoryMark`, `_answerSpeakerWord`'s consumption mark all retired); `_readDirectorState` overlays the store's `played` onto both (rows `via:"story"`, stripped in `_writeDirectorState`), so every legacy reader (dialogue offers, chains-in-motion in hex-travel, the Visualizer, `_listOpenInvites`) sees the store. **Time (owner rulings R1/R2/R7):** `_applyBeatTimePoints` returns early (beats spend no days; the WME unlock-time debit is off); the turn-driver day-funding pass is off (`budgeted = null` — planned activities cost marks + one turn, never hold); the ledger settle carries no debt (progress resets to 0; whisper says "the chapter turns"); Turn Driver confirm shows "Day X of Y — the chapter turns"; the beat editor's timeScale/timePoints tooltips say retired. Travel legs still move the Day chip (a prompt). **Travel tables per act (2026-09-14):** `runRandomTable` now honors entry `conditions.phaseGte`/`phaseLte` (act window against `storyPhase`), entry `once` (spent when the story store holds the beat as played), and the drawn beat's OWN `inject.requires` + seals (`_beatRequiresMet`) — so one table per terrain suffices and each entry says which acts it rides in. Seeder `tools/seed-hum-travel-entries.macro.js` (DRY_RUN): the five later Sarmoung Hum rungs as rare once-draws in their act on their terrains; `tools/patch-hum-tent-rails.macro.js` (DRY_RUN): the first rung on rails out of Allesh-Gilliam (First Night → tent → Crossroads), overheard between Bit and Coll, `pacing.ambient`. **Entry guards (2026-09-14):** `executeBeat` refuses, on every path except `ctx.force` / `source:"phase-door"`, (a) a beat whose act (`storyPhase` gate) is AHEAD of the story ("not yet" whisper — routing never checked gates; an orphaned Act-1 church intro walked the table into Act 2), and (b) a beat authored `inject.hardGate: true` whose `inject.requires` are unmet (a hub choice may point at it; the door stays shut until earned — the Fixit back stairs / generator). `api.campaign.story.forget(beatIds)` strikes stray played marks and re-derives. Tools: `scrub-orphan-beats.macro.js`, `patch-review-beats.macro.js`, `patch-hum-tent-rails.macro.js` (all DRY_RUN). **`bin/ft-replay-story <save|bundle> [--max=N] [--act=N]`** (→ `tools/replay-story.mjs`, 2026-09-14) = OFFLINE STORY REPLAY: walks a save the way the table would (takes the NOW card's next, runs it through seal / future-act / hard-gate, follows routes as the model does, rides to towns on arrival, advances at THE TURN), stopping on refusals, loops or dead ends. Run it after every seeder and before every playtest; it found every Act 0→1 seam bug of 2026-09-14 in seconds. **Lint D-rules** (`lint-campaign.js`, requires story-model via Node ≥22.12 `require(esm)`): D01 quest beat without a declaration · D02 declaration names an unknown quest/chapter/role · D03 quest with no start / no closer (ninth_guest, sarmoung_hum, offices exempt) · D04 chapter with beats but no ending · D05 `questBucket` gate on a `word_*` ticket or an unmapped id. **Sparks in the world from a beat (2026-09-13, owner ruling "a quest's closing ending integrates its hex's spark"):** `worldEffects.hexSpark = {action: seat|integrate|corrupt|repair|unseat, key?, state?, hexName?|targetHexUuid?}` (or a LIST) → `game.bbttcc.api.tikkun.hex` (integrate debits Reach + lights a Lamp; corrupt refuses temples; integrate refuses corrupted until repaired), GM whisper "Sparks in the world". Seeder `tools/seed-spark-endings.macro.js` (DRY_RUN): moves the three Gevurah kinds onto the three Lost Statue hexes (worthy = integrate, force = corrupt), seats the three Hod kinds CORRUPTED on the flooded towns (calm endings repair; Echoes Reconstituting repairs + lights all three), Chokmah conceptual at PolygonWood.a (Hidden Vault), `KT_SPARK` at Khezek-Tor (default Yesod animate — the Yesodium mine), stamps every town settle / trail stop ending / Vault / Mall / Gloomgill closer, and drops the Garden count's duplicate `complete` of the Statues quest. **Turn contract** — `bbttcc:advanceTurn:begin` / `:end {apply}` emitted by `bbttcc-territory/scripts/turn-driver.js` (wrapper stack: driver → tracks → garrison upkeep → turn-requests). **Every listener must gate on BOTH `apply` and `game.user.isGM`.** Census of listeners lives in each module's section; one known mis-gate (campaign ledger phase-door) is 🔴 in Appendix A.

**Actor kinds** — `game.bbttcc.api.actorKind(a)` → `faction|rig|facility|steward|monster|npc`; defined in the **system** (`systems/fourththing/actor-kind.js:41`, mount `:98-104`), cached at `flags.fourththing.kind`. **2026-09-04:** any actor carrying `flags.fourththing.rfi.actor.lineage` or `.bestiary` resolves to `monster` (step 2b) — `isMonster(actor)` exported. **Monsters have NO aptitude requirements:** `skillRollWithRank` rolls them at Trained-tier mechanics with label "Innate" (no fumble; stored bonus unchanged), `_ftEquipProficiency` returns null (no "Not proficient" banner), armor scales at rank ≥2 (full bonuses). **NPC sheet lineage strip** (`_ftLineageStrip` → `lineageStrip` ctx; `npc-sheet.hbs` under the level display; `.ft-lineage-strip` css): Lineage · sub · Title · Qliphah · Envelope (tier/bracket/role) · Type · Bounty · Resolved. Factions are `type:"npc"` + `flags["bbttcc-factions"].isFaction`.

**Combat seam** — `game.bbttcc.combat.{applyDamage, resistsForcedMove, ...}` slots created null by core (`bbttcc-core/scripts/module.js:48-54`), filled by the system (`systems/fourththing/module.js:15535-15544`). Pre-damage extension point: `game.bbttcc.combat.registerDamageInterceptor(fn)` (`bbttcc-core/scripts/module.js:55-70`); bbttcc-structures is the reference consumer.

**Two "campaign" namespaces** — `api.campaign` (bbttcc-campaign) and `api.campaigns` (created by travel, `hex-travel.js:1763`, holds the LIVE beat injector + a `runBeat` alias). Resolve via fallback like `bbttcc-encounters/scripts/trigger.manager.js:33` does.

---

## 1. systems/fourththing

*Roll for Initiation (Bad Eden Core Rules)* v0.6.1. Actor types `character` ("Steward") / `npc` / `rig`; item types `power weapon armor gear feature feat class subclass race species spark`. Six attributes (violence/intrigue/presence/body/mind/soul), 23 aptitudes, init `2d10x10 + intrigue + bonus`. Standalone-safe: every BBTTCC call guarded (`bbttcc-bridge.js:10`).

### API — `game.fourththing.*` (assembly `module.js:13302`)
- `.constants` = the `FT` table (`module.js:177`). `.items` = RfiItems unified flag schema (`rfi-items.js`). `.craft` = RfiCrafting (`inventory/recipeFor/difficulty/canCraft/recipesAvailable/tryCraft`). `.harvest` = RfiHarvest (`scanScene/attempt/markNode`). `.pricing` = RfiPricing (`rfi-pricing.js`) — incl. **`computeCreaturePricing({tier, bracket, lineage})` → `{bounty, hire, ransom, currency}`** (2026-09-04 creature rubric: `CREATURE_TIER_BASE` 5/10/20/40 × `CREATURE_BRACKET_MULT` light 1/medium 1.5/heavy 2/boss 3; hire ×1.5, null for boss; `LINEAGE_DEFAULT_CURRENCY`); `computeBossPricing` is a back-compat alias (the original was deleted in `e26e10d`). Bounty is credited to the pool matching the resolution method — payout engine NOT yet built (see `BESTIARY_AUDIT_2026_09_03.md` §6).
- `.stack.{identity, findTarget, orCreate, merge}` — inventory stacking (`module.js:8574-8644`). Market delivery uses `orCreate`; `merge(actor)` = one-time dupe collapse.
- `.equipProficiency(actor, item)` → `{skillKey, rank, trained}` (`module.js:10353`) — the Market untrained-chip probe.
- `.surge.{itemFor, openSpendDialog, spend, availableFor}` (`module.js:13350-13372`) — per-class Surge spend tables live `module.js:4176-6600`.
- `.echoAssets.{get, openManager}` + `.roster.{get,set,delete,list,count,open,slug,generate}` + `.spotlight.{can,get,invoke,consume,clear,pickFromCrew,quickInvoke}` (`module.js:13383-13456`, engine `:12692`).
- `.rolls.*` — **`checkFormula({mode,explode})` → `"2d10x10" | "3d10x10kh2" | "3d10kl2"` and `flatCheck({bonus,dc,mode,explode})` → `{roll,total,baseDice,explosions,doubleTen,ok}` (2026-09-06: the canon die for ACTOR-LESS module checks — campaign OP checks, siege duels/relief/trojan, Tikkun ritual, Ruin-to-Renewal all moved off 1d20 onto these; no Surge banked)** · `applyOvershoot` (`:13692`), `bankRestraint/consumeRestraint` (`:13780/:13802`), `attributeTest` (`:13822`), `skillCheck` (`:13988`), `magicTest` (`:14033`), `resolveManifestationAttack/Save/Contest` (`:14256/:14347/:14491`), `misfireRoll` (`:14622`), `attackTest` (`:14659`), **`_applyDamageToActor`** the canonical damage/heal writer (`:15171`), `applyDamageFromButton` (`:15578`).
- Flat helpers: `castManifestation` (`:7109`), `wizardV2`/`openManifestationWizardV2` (`:15753`), `ftOpenEngageDialog` (`:1137`), `noiseBand/noiseBands/noiseBite` (`:910/:918`), `radiationBite` (`:10045`), `toggleCondition` (`:16139`), `defeatState` (`:16181`), `applyManifestationStates` (`:16192`), `applyEffectsToTarget` (`:6781`), `ftPlayAutoAnimation` (`:1574`), `syncTokenVision` (`:17963`), `classifyStewardItems`, `manifestationGlossaryHTML`, `createManifestationItemData` (`:8441`).
- `.actions.{somaBreak, sceneBreak, fiat, lesserManifestation, endScene}` (`:15748-16088`).
- `.deathMech.{enterLastStand, stabilize, crossThreshold, rollLastStand, addFailures, redeem, forceReincarnation}` (`:16450-16685`).
- `.bloodDebt.{get, add, clear, probe}` (`:16719-16770`).
- **`.darkness.{get, band, bite, gain, wash, setTaint, fragments, addFragment, face}`** + `.darknessBite` (Descent Engine D1, 2026-09-02 — `DESCENT_ENGINE_SPEC.md`): personal Darkness 0–10 on `system.darkness{value,taint,fragments}`, the ONLY sanctioned write path; bands Clear/Shadowed/Umbral/Nadir/Threshold via `FT.DARKNESS_BITE` (+ `FT.CONDITIONS.darkness*` display entries); Umbral+ dims max Clarity −2 (floor 1) in derivation; overflow past 10 scars taint +1; `face()` is the only taint heal (−1, −2 with `darknessSpikes`); sheet chip beside Radiation. **D2/D3 shipped 2026-09-03**: automatic sources — T2+ cast misfires (+1, via NEW hook **`fourththing.misfire` {actorId, tier, band, d10, source: cast|manual}** emitted at both `ftResolveMisfire` cast sites), Umbral+ Chaos casts (misfire softened one band AND +1 Darkness — the boon and its price, `castManifestation` bias calc), dark-hex travel (`bbttcc:afterTravel` listener: dest per-hex darkness ≥5 → +1 to the steward, once per hex per world turn, log `flags.fourththing.darkHexLog`), qliphothic damage ≥6/scene (+1 once, accumulator `flags.fourththing.qliDark`, chokepoint `_applyDamageToActor`), beat tags **`darkness:+N`** / **`fragment:<qliphoth>`** (`bbttcc:beat:resolved` listener, GM-gated); washes — Soma Break drains value to taint floor. `soulSavePenalty` (−2 Nadir+) live in `resolveManifestationSave` for soul saves. D3: fragment ledger UI on the sheet (open/faced + GM-only **Face** button, action `ftFaceFragment`) + public band icon as a marker ActiveEffect synced on `fourththing.darknessChanged`.
- `.rig.{board, disembark}` (`:25856`, impls `:23302/:23536`); `.maneuvers.inferFireMode` (`:25918`); `.vfx.{playCombat, playSweepArc}`.
- `.ae.{register, registerMany, get, has, all, byCategory, docs}` — curated AE-key registry, runtime-extensible (`ae-keys-registry.js`, picker `ae-key-picker.enhancer.js:132`). `.ae.docs()` prints the whole table.
- `._progression.*` (levelUp, **levelDown (2026-09-08 — GM-only ± Initiation on the sheet; exact reversal via the level ledger `flags.fourththing.levelGrants[L]` + item stamp `flags.fourththing.grantedAtLevel` + spend ledger `flags.fourththing.skillSpends[]`, all written since 2026-09-08; earlier levels reversed by inference — unlock-gate scan + GM-picked faculty; re-arms `director.rearmLevelPrompt`, resets Epic convergence under 18)**, tier math, path features, `grantStarterManifestations(actor)` — grants the path+doctrine starter manifestation kit from `bbttcc-master-content.items` by `flags.fourththing.starterKit {path, doctrine, core}`; called by the sheet's "+ Apply Path Features" button and auto-link's creation wizard; idempotent by item name — `ft-progression.js`), `._classAutomation.*` (~60 class dialog openers + `FEATURE_ROUTER`/`dispatchFeatureAction` — `ft-class-automation.js`), `.migrations.{run,list,stored,current}` (`migrations/index.js` — v1 baseline, v2 stamp-actor-kind, v3 repair-aptitude-grants).
- Outbound to modules: `game.bbttcc.combat.applyDamage/resistsForcedMove` (`:15535`), `game.bbttcc.api.raid.battleScenes.{list,current,bind,unbind,activate}` (`:26920`), `actorKind` (actor-kind.js).

### Hooks emitted
`fourththing.noiseChanged` (`:966`) · **`fourththing.darknessChanged {actorId, before, after, taint, source}` / `fourththing.fragmentGained` / `fourththing.fragmentFaced`** (Descent D1) · `fourththing:itemAnimated` (`:1583`) · `fourththing.manifestationCast` (`:7851`, payload incl. `reachPath`, `success`) · `fourththing.overshoot` (`:13773`) · death ladder: `enteredLastStand/stabilized/thresholdCrossed/redeemed/reincarnationPending` (`:16471-16711`) · `bloodDebtChanged/Cleared` (`:16747/:16763`) · `targetLeftReach` (`:17779`) · `revivedFromDying` (`:17900`) · `featureDispatched` (`ft-class-automation.js:7209`) · `bbttcc:rig:damaged/destroyed` (`:15502/:15505`) · `bbttcc:enlightenmentChanged` (`bbttcc-bridge.js:1854`).
Listens (notable): `bbttcc:advanceTurn:end` (`:17827` reminder card), `bbttcc:raid:roundCommit` (`:25797`), `bbttcc:infiltration:*` (`:26389`). Socket: `system.fourththing` + `module.bbttcc-raid` share one GM relay (`:26913`; msg types `ft-boardSelection … ft-grantClarity` `:26795-26891`).

### Schema highlights
- Character: `magic{clarity,noise,sephirah}`, `derived{integrity,stress,guard,evasion,resolve}`, `defenses{resistances,immunities,vulnerabilities}` (⚠ RFI resists live HERE, not 5e `traits.dr`), `lastStand`, `bloodDebt`, `radiation{rp}`, `darkness`, `tikkun`, `actions{...movementBudgetFt}`, 15 conditions incl. 4 radiation rungs.
- Rig: `identity{mobility,state,factionOwnerId}`, `crew{slots,capacity}`, `integrity{value,max,tier,bracket}`, `travel{speed,range,hazardResist,domains,depthRating}`.
- Flags (second schema): `flags.fourththing.rfi.item.*` (price/tech/charges/**chargesMax**/consume.effects/origin), `flags.fourththing.harvest`, `flags.fourththing.discipline.*`, `flags.fourththing.kind`, `flags.fourththing.combat.bonusManifestationUsedThisRound`. ⚠ `actor.system` is AE-mutated in place — writes must go through the SOURCE reader (`module.js:994`).

### Settings / tools
World settings: `overshootEnabled/overshootTierGate/overshootSkillChecks/autoApplyEffects/ftAoeTargetPicker` (`:13605-13638`) + hidden `schemaVersion`. No chat commands. Packs: `starter-manifestations`, `surge-abilities` (+137 `_source` JSONs). `tools/`: 8 paste macros + 6 node build scripts (AA autorec, surge ability builders).

### Dormant/broken → see Appendix A items 1–5.

---

## 2. bbttcc-core

*Strategic Layer Core* v13.1.17 — namespace bootstrap, GM write/audit layer, combat adapter seam, central help registry, global window/theme defaults (`be-theme.css` et al.), and the **Manifestation Bridge** (steward↔faction value transfer).

### API
- ⭐ **`game.bbttcc.api.gmExec.{register, call, primaryGmId, types}`** (`gm-exec.js`, 2026-08-29, Phase 1 of the engine roadmap) — THE seat primitive: `register(type, handler)` GM-side (handler(payload, meta{fromUserId,fromUserName,local}) → serializable result, MUST validate + return fast — kick long work fire-and-forget); `await call(type, payload, {timeoutMs=15000})` from ANY seat (GM = local; player = socket relay on `module.bbttcc-core` to the primary GM w/ ack/timeout). API published at PARSE time (load-order safe); listener arms at ready. Registered types: `op.commit` (bbttcc-factions — makes ALL bank writes player-seat-safe: travel legs, passenger cuts, dialog spends), `travel.hexEnter` (bbttcc-travel — arrival beats run GM-side, ack is a receipt). ⚠ bbttcc-core module.json now has `socket: true` + gm-exec.js first in esmodules (manifest change 2026-08-29 → restart done; clients owe full cache bust).
- `game.bbttcc.api.core` + null slots for factions/territory/characterOptions/campaign/encounters (`module.js:23-30`); `game.bbttcc.combat` slots + `registerDamageInterceptor` (`:48-70`).
- `game.bbttcc.help = {register, entry, tip, dict, apps, applyTooltips}` + `{{bbttccTip}}` helper (`bbttcc-help.js:77-88`) — one source of truth for tooltips AND Operator tours.
- `game.bbttcc.api.gm.{setWorld,setFaction,setActor,setHex}` (`api.gm.js:406-411`) — allowlisted structured-patch writes (`ALLOW` `:111-139`), audited to `bbttcc-core.gmAuditLog` (250-ring), GM-whispered. `setHex` delegates to `territory.gmSetHex`.
- `game.bbttcc.api.story.gottgait.{getState,setState,updateState,logBeat}` (`story.gottgait.js:58`) — state store + beat log only (engine removed).
- `game.bbttcc.api.bridge.{open, manifest, manifestHp, backing}` (`bbttcc-bridge.js:927-936`). `manifestSacrificeToFactionOp` (`:361`) — deposit-marks-first, refund-safe; sacrifice types `integrity/stress/aptitude/manifestation` (tier-priced, `:390-429`; aptitude burn restores on Soma Break via `flags.fourththing.aptitudeBurn`). `spendFactionOpForRoll` (`:492`) — faction OP backs a real RFI roll (+2/OP flat or 1d6/OP dice). Blood Debt canon at `flags.bbttcc.identity.bloodDebt`, prefers `game.fourththing.bloodDebt.add`.
- Global: `Dialog` replaced resizable-by-default (`window-defaults.enhancer.js:51`); all `.window-content` get scroll defaults.

### Settings
`gmEditMode` (world, visible — gates territory build-unit buttons) · `gmAuditLog`, `worldState`, `gottgaitStoryState` (hidden). No hooks emitted, no tools.

⚠ `bbttcc-core.worldState` (api.gm) vs `bbttcc-world.worldState` — **two divergent world-state spines**, nothing reconciles them (Appendix A #14). `combat.dnd5e.js` is a no-op in the RFI world by design.

---

## 3. bbttcc-factions

*Faction & OP Engine* v1.3.17 — faction sheet (V1 `ActorSheet` on `npc`, emits `renderBBTTCCFactionSheet`), OP banks, stockpiles, relations, atomic exchange, async trade inbox, doctrine-as-items, starting packages/wizard, Rig Console, ~14 sheet enhancers.

### API
- **`game.bbttcc.api.op`** (`op-engine.js:416-425`): `preview(factionId, deltas, ctx)`, `commit(...)` (underflow always refused; over-cap refused only when the delta increases an over-cap bucket; `ctx.allowOvercap` bypasses), `KEYS`, `OP_TO_MARKS`, `formatMarksAsOP*`, `opToMarks`, `runMarksMigration` (⚠ epoch guard `MARKS_MIGRATION_EPOCH_MS = 2026-08-15` after the ×10 phantom-treasury incident). **(2026-09-06 marks ruling: `fmt`/`fmtNum` added; `formatMarksAsOP*` now render MARKS — "N marks"/"N" — every display surface speaks marks; see `MARKS_EVERYWHERE_PLAN.md` phase 2)** **Phase 1 (same day): the OP↔marks ratio has ONE authority — `systems/fourththing/rfi-pricing.js` `MARKS_PER_OP`, published as `game.fourththing.constants.MARKS_PER_OP`; `api.op.OP_TO_MARKS` (ready snapshot) and `api.op.marksPerOp()` (live) read it; every module reads through one of those — never a literal.** **Phase 3 (same day): every authored/input OP quantity is now MARKS — market catalog (auto-migrated ×10 once via `bbttcc-market.catalogMarksMigrated`), strategic-throughput yields, agent previews, Tikkun yield/ritual, bridge dialog, courtly/infiltration/siege/backing inputs (step 10, mechanics per 10 marks). Encounters scenario internals still OP at read/save boundary (phase 4).** **Phase 4: `formatMarksAsOP`/`formatMarksAsOPNumber`/`opToMarks` RETIRED; encounters scenario internals now marks; `bbttcc-campaign/tools/lint-units.js` (`bin/ft-lint-units`) repo sweep = 0 hits.**
- **`api.factions.exchange`** (`exchange-engine.js:415`): `plan` (pure preview), `share` (allied send, zero friction, needs mutual `allied`), `trade` (tier friction, worst-of-two). Resource shape `{marks:{...}, buildUnits, materials:{...}}`; materials move friction-free. True atomicity with rollback (`_applyDeltasAtomic` `:210`).
- **`api.factions.relations`** (`relations-api.js:193`): `get/set/tier/list/canTrade`; ladder `at_war(0)…allied(5)`; `canTrade` needs both ≥ neutral.
- **`api.factions.stockpile`** (`stockpile-api.js:236`): `get/qty/list/adjust/depositFromCharacter/withdrawToCharacter` → `flags.bbttcc-factions.matStockpile`.
- **`api.factions.pending`** (`pending-trades.js:329`): trade inbox state machine (`pending→accepted|declined|countered|expired`, 7-day TTL, stored on recipient).
- `api.factions.{setMorale,setLoyalty,bumpMorale,bumpLoyalty}` (`faction-meters.enhancer.js:137`).
- `api.victory.{computeVPGain, applyVPGain}` (`vp-engine.js:35/:71`) — `0.5·Unity+0.3·Morale+0.2·Loyalty`, cap +3/turn.
- `api.quests.openQuestLog({factionId})` (`quest-log-app.js`). **Invitations tab (2026-09-12):** the Quest Log lists every open "wants a word" invitation (from `api.campaign.invites.list()`) as rows with an **Accept the invitation** button on ANY seat — player clicks relay to the GM through bbttcc-core gmExec (campaign setting `invites.playerAccept`, default on). Opens on the Invitations tab when any are open; refreshes on `updateSetting` for `bbttcc-campaign.quests` / `.directorState` (reaches every client). **📍 Where pills (2026-09-14):** the selected quest's detail head lists every town-hub door its un-completed beats point at (`api.travel.doors.forQuest`); `data-ql-act="goto-door"` pans this seat to the door on the hub map and pulses it — "look at the quest log, there it is, click".
- Main module (`module.js:4096+`): `startingPackages/listStartingPackages/getStartingPackage` (PKG_STANDARD_V1 `:4130`), `doctrine.{list, ownedKeys, grant}` (`:4197`), `applyStartingPackage` (`:4225`), `createFactionFromPackage` (`:4405`), `previewStartingTerritory`/`assignStartingTerritory` (`:4576/:4642`), `openCreationWizard` (`:4780`).
- Apps: `game.bbttcc.apps.{AlliedSendApp, TradeApp, QuestLog, RigConsole}`.

### Hooks
Emits `bbttcc:economy:share/exchange`, `bbttcc:stockpile:changed`, `bbttcc:trade:pending/settled`, `bbttcc:relationship:changed`. Listens `bbttcc:advanceTurn:end` (momentum reset, pressure, victory gate — all GM-gated), `bbttcc:beforeTravel`.

### UI / settings / tools
Five-tab faction sheet (Overview/Activities/Assets/Relationships/War Logs); Rig Console AppV2; Momentum spend menu (bank +1 per nat-10 OP check; spend 1/2/3/5 — `module.js:3900-4025`); GM Advisor dialog; trade inbox/history/stockpile/meters/dashboard enhancers. Settings: `pendingTradeSweepMinutes`, `autoRelationshipNudge`, `fairTradeTolerance`, `nudgeCooldownHours`. Tool: `repair-marks-double-migration.macro.js` (no DRY_RUN — writes on run). Tool: `repair-onboarded-faction-parity.macro.js` (2026-09-10, DRY_RUN=true) — brings onboarding-founded factions to wizard-package parity: seeds missing `opCaps` at the tier band (floored to what is banked) and morale+loyalty below `RESEED_TRACKS_BELOW` (25; set 1 for zeros-only) to 25; War Log milestone `parity_repair`.
**PENDING-KEY REGISTRY — `bbttcc-core/pending-keys.json` + `bin/ft-lint-pending` (item 3, 2026-09-12):** every queued key a writer may set (`hex.turn.pending.*`, `faction.turn.pending.*`, `faction.bonuses.nextTurn.*`, `faction.bonuses.*`) is declared with its CONSUMER (file#symbol) and a status: live / retired (any remaining writer fails P2) / unwired (declared debt, reported). P1 = undeclared writer, P3 = a live consumer whose file or symbol vanished (registry rot). First sweep retired 7 Trade-Yield writers + supply-cache `logisticsDelta`, redirected Gather Intel / Policy Reforms / Mass Mobilization from the dead faction `turn.pending.nextTurn` to `bonuses.nextTurn`, and gave Training Drills' `nextTurn.moraleBonus` a consumer (the tick). **Rulings 2026-09-12 (registry now 0 unwired):** `bonuses.nextTurn.attackDC` = the attacker-side next-raid DC shift (Policy Reforms −1, Gather Intel −2) read by `module.raid-console _rcAttackerNextBonus` at Add Round (folded into baseDC) and in the projection breakdown ("+ Attacker −2"), expired by `tickFactionBonuses`; the `policyReform` / `intelAdvantage` flags and the two `dc*Bonus` keys are gone. `enemyLoyaltyDelta` is consumed: `mods.enemyLoyalty` SUBTRACTS from `facts.hex.loyaltyScore` (a rival's `loyalty_program` on your hex raises its unrest odds, makes it the least-loyal pick, and drags the territory pull); `facts.hex.enemyLoyalty(tf)` exposes it. The `worldEffects` entry was a false positive — it is the raid console's per-round maneuver world-effects list under `r.meta.intents.pending`, consumed at commit — and is out of the registry. **FACTS LAYER — `game.bbttcc.facts.faction` (2026-09-12, "fix the boat" item 1):** op-engine.js is the ONE home for tier / caps / logistics-capacity cap — `facts.faction.{tier(actor), caps(actor), capBand(tier), logisticsCapMarks(actor), CAP_BAND, LOGI_CAP_FLOOR}`; the band `[50,70,90,110,130]` and floor `[70,70,90,110,130]` are ES-exported (`TIER_CAP_BAND_MARKS`, `LOGISTICS_CAPACITY_FLOOR_MARKS`) and imported by module.js / the tier button; `_readCaps` delegates to `factionCaps`. The Director's cap raise and turn-driver's logistics pass read facts at run time with NO local literal. `bin/ft-lint-facts` (→ `bbttcc-core/tools/lint-facts.js`) fails the repo on a second band literal (F1), tier re-derived from `tierFromBadge` outside op-engine (F2), cached hex-yield reads (F3), `mods.loyalty` reads / loyalty-table copies outside territory main.js (F4), retired route counters (F5); tag a legitimate derivation `facts-ok`. Must read 0 hits before any commit touching OP/hex code. Tier advancement (wired on 2026-08-28): stability counters accrue per Apply turn (`faction-tier-stability`), `api.factions.{getTierAdvancementReport, canAdvanceTier}` (`faction-tier-advancement-api`), GM Advance Tier button raising opCaps to the marks band (`faction-tier-advance-button`). **`api.factions.tier.{get, set(actor, tier, {note}), band}` (2026-09-08)** — the one sanctioned tier WRITE: raising lifts opCaps to the band, lowering drops them to the band but never below banked marks, War Log milestone, returns `floorWarning` when below `directorState.factionTierFloor`; surfaced as a Tier select in the faction sheet's GM Edit fieldset. Fresh-run tool: **`bbttcc-campaign/tools/reset-factions-from-the-top.macro.js`** [DRY_RUN=true] — coalition/all factions → T0 via the setter + VP/unity/progression zeroed + Director floors and latched level prompts cleared, flags backed up to JSON first. ⚠ Runs its own tip dicts instead of `game.bbttcc.help`.

---

## 4. bbttcc-banks

*Banks & Loot* v0.1.0 — one **Vault** primitive → personal bank (items only), faction/coalition treasury (items in flag + marks in opBank), on-canvas loot containers. Doctrine: **marks never sit on a steward.**

### API — `game.bbttcc.api.banks` (`vault-core.js:446-465`)
`transferVault({source,target,items,marks,context})` (atomic credit-before-debit + rollback) · `deposit/withdraw/listVault` · `factionTreasury` · `accessibleFactions` · `coalitionLeadFactions/setCoalitionLead` · `openPersonalBank/openFactionBank/openLoot/openLootConfig`. VaultRef kinds `inventory|personal|faction|coalition`; bankable = physical gear or `bound|enduring` manifestations. Emits `bbttcc:vault:changed` (`:363`).

Loot flavors `corpse|chest(tinkering DC12)|terminal(hacking DC14)|cache`; **auto-loot on defeat** (npc integrity/stress → 0 becomes free corpse, active-GM-only, `loot.js:352-401`); Token HUD 🪙/✋ buttons; GM socket relay on `module.bbttcc-banks`. No settings. Tool: `make-salvage.macro.js` (confirm-dialog gated) — salvage refines into marks on faction deposit.

---

## 5. bbttcc-territory

*Hex Territory Map* v1.3.20 (~20k lines, biggest module) — hexes-as-Drawings, terrain/conditions/resource nodes/fog, the **turn pipeline**, resolution/outcome matrix, garrison upkeep, build units, Townbuilder, quest links, GM toolbar + player HUD, Territory Dashboard / Campaign Overview.

### API
- **`api.territory`** (`main.js:3182-3230`): `gmSetHex`, `createHexAt/openHexConfig/claim`, `normalizeTerrainKey/buildTerrainObj`, `leylines.{tierRank,getFactionTierKey,gateUsable,resolveRemoteAdjacency}`, `resourcesToOP`, `recordHexImprovement`, `describeHexModifier/MOD_DESCRIPTIONS`, `computeHexNextSteps`, `traceHexProduction`, `refreshCapitalOverlays`, `refreshAllHexStyles({allScenes})`.
- `openHexSheet` (canonical: `bbttcc-hex-sheet.enhancer.js:1922` — wins over the superseded main.js copy), `holdings` (`:500` — deploy/recall rigs+bosses to scenes, caps, bonuses), `buildUnits.{get,canAfford,spendForAction}`, `townbuilder.{open,getSettlement,ladderFor,LADDER}`, `fog.*` (`hex-fog.layer.js:288`), `harvestHexNode/regrowHex/addHexNode/deleteHexNode/openAddNodeDialog`, `applyOutcome` + `saltHex` (`resolution-engine.js:40/:266`), `getUnityBonusReport`, `questLinks`, `openCampaignOverview`.
- **`bin/ft-sim-economy`** (→ `tools/sim-op-economy.mjs`, 2026-09-11) — offline OP-economy simulator: parses the live constant tables out of the engine sources at run time (RES_TO_OP, TYPE_BASE, SIZE_MULT, LOGI, upkeep tables, cap band, activity prices, rfi gear pricing — a parse miss prints DRIFT?), mirrors the turn pipeline (plan spend → regen × loyalty pct × overextension → tier floor → loyalty drift → upkeep + integration bonus), and plays 5 policies (expand / alternate / turtle / story / gear) for N turns against the 7 owner targets. Knobs: `--price-mult --econ-price-mult --occupation-mult --spend-order before|after --sprawl-exp --no-recipes --reward-marks --gear-every --max-hexes --tier-floor-turn`; **player-action drains (2026-09-12):** `--travel-legs N --travel-terrain <TERRAIN_TABLE key>` (parsed from hex-travel.js; own/allied dev-6 hexes are free in the engine), `--raid-rounds N --raid-pool <ch> --raid-staged M` (10 marks primary per round), `--courtly-rounds N` (20 diplomacy each); scorecard adds a `short` column = turns a drain could not be paid. `--parity <save.json> --faction <name>` recomputes a real faction's regen/upkeep/logistics from a world save (2026-09-11: exact match on all 7 regen channels + 4 upkeep vectors vs the engine's own log). Since ruling B shipped, the tool parses `PRICE_MULT`, `RECIPES` and the occupation `phaseMult` FROM the engine (`--price-mult` / `--no-recipes` / `--occupation-mult` override them); its own RECIPES literal is only a fallback. Report: `OP_ECONOMY_SIM_2026_09_11.md`.
- **`api.turn`**: `advanceTurn`/`executeOPRegen` (+aliases) (`turn-driver.js:1782-1791`), `computeLogisticsPressure(+ForFaction)`, (logistics CAP resolved by `_logisticsCapMarks` 2026-09-10, floored by `_LOGI_CAP_FLOOR = [70,70,90,110,130]` since 2026-09-12 so T0 factions read 7 capacity, not 5 — spend caps untouched; explicit `opCaps` → `opCapPer` → tier band `[50,70,90,110,130]`, mirroring op-engine `_readCaps` — the old raw-flag read fell back to the BANK for any faction without an explicit `opCaps` flag, which is how onboarding-founded Errata ran STRAINED 1.33 on the same moves wizard-built Sweet Release ran at 0.67), `enqueueRequest`, `processRequests`. Wrapper stack: driver (`installDriver` `:1521`) → tracks (`advance-turn.tracks.js:1018`) → garrison upkeep → turn-requests.
- Globals: `BBTTCC_OpenTerritoryDashboard`, dashboard/overview ctors, `window.BBTTCC_RESOLUTIONS`.

### Hooks
Emits `bbttcc:advanceTurn:begin/end{apply}` (`turn-driver.js:1557/:1630`), `bbttcc:hexImprovement`, `bbttcc:territory:hexUpdated`, `bbttcc:gate:unlocked`. Listens `bbttcc:beat:resolved` (gate-unlock listener, GM-only writer), own hooks for facility turn effects / fog / dashboard.

**Hex loyalty/morale — ONE field (ruling 2026-09-12):** canonical = `flags.bbttcc-territory.mods.loyalty` / `mods.morale` (written by `applyHexPendingSweep` numeric deltas, `processRequests` cleanse, recipe fx). The top-level `tf.loyalty`/`tf.morale` were never written by anything (census 2026-09-12: 0 of 157 hexes) — the intel whisper now reads `mods.*` with `tf.*` as fallback. **Hex → faction loyalty (ruling 2026-09-12):** `advance-turn.tracks.js doHexLoyaltyPull` (before Loyalty Phase 1) — hex score = `mods.loyalty` + registry loyalty of its modifiers (Loyal Population +2 / Hostile −2 / Well-Maintained +1 / Damaged Infrastructure −1); mean over owned hexes, clamped ±3, is added to faction loyalty each Apply turn (GM chat "Territory → Loyalty"). Loyalty Phase 2 unrest + infra damage strike the LEAST loyal hex, and their ODDS follow its score (ruling 2026-09-12): unrest 10% ± 5%/point clamped 2–60%, rolled when faction loyalty < 30 OR the lowest hex is ≤ −3; infra damage 25% ± 5%/point clamped 5–75%, still gated on faction loyalty < 15. The chat line names the hex score and the odds rolled. **Sharing trace (T7, 2026-09-12):** `exchange-engine` records every Allied Send / Trade on both factions at `flags.bbttcc-factions.sharing {ledger≤60, lentTo, borrowedFrom}`; `api.factions.sharing.get(id)` (also `exchange.sharing.get`) → `{lentTotal, borrowedTotal, net, borrowStreak, …}`; `tracks doSharingPressure` (after Phase 2): a net borrower 3+ consecutive world turns takes −1 Loyalty/turn + GM whisper + war-log `sharing_pressure`. **Occupation upkeep × 0.5 (ruling B, 2026-09-11)** — `territory-garrison-upkeep.enhancer.js` occupation `phaseMult` 1.5 → 0.75 (short 1.0 / full 0.3 unchanged). **FACTS LAYER — `game.bbttcc.facts.hex` / `facts.routes` (2026-09-12, items 1+2):** territory main.js is the ONE home for a hex's identity-derived truths — `facts.hex.{type, size, integration, loyalty, morale, loyaltyScore, baseVector, resources, HEX_MOD_LOYALTY, TYPE_BASE, SIZE_MULT}` and `facts.routes.{trade(factionId), supply(factionId)}` (stored edges only). **Yield is COMPUTED ON READ:** `facts.hex.resources(tf)` = manual-override base or Type × Size ladder → + sephirah → × modifiers; `flags.resources` / `calc` / `effectiveCached` are a DISPLAY CACHE (Hex Config save + `recomputeHexResources` refresh it; the audit macro compares cache to fact). Consumers rewired: turn-driver income + stockpile derivation, tracks Build Units, hex sheet resource list, intel whisper, tracks `hexLoyaltyScore`, tracks trade-route logistics bonus (now counts stored edges — the nearest-six guess is gone; `deriveTradeRouteCountFrom{Edges,WarLogs,Scene}` deleted). `_logisticsCapMarks` delegates to facts.faction. sim-op-economy parses CAP_BAND/floor from op-engine and HEX_MOD_LOYALTY from main.js. **Hex resource recompute (2026-09-10)** — `api.territory.recomputeHexResources(doc, {source})` re-runs the Hex Config save's calc (Type×Size ladder + sephirah adds × modifier multipliers: Trade Hub ×1.5 trade, Well-Maintained ×1.25, Loyal Population ×1.15, …) into `flags.resources`, the field `advanceOPRegen` reads. Called by `applyHexPendingSweep` for every hex whose modifier list changed and by the raid adapter `setHexModifiers` — before this, a bought Trade Hub changed no income until someone saved the hex sheet. Manual-override hexes keep hand-set resources; `{dryRun:true}` (2026-09-12) computes and returns `{changed, manual, before, resources, base, multipliers, type, size}` without writing. **Tool `audit-hex-resources.macro.js` (2026-09-12, DRY_RUN=true):** sweeps every hex through that dry run and tables MISMATCH (stored ≠ own ladder; fixed when live), MANUAL (reported, never touched) and MIS-SIZED (owned/occupied at size none) — born of Bedlam Thirdword carrying a farm×outpost ladder under a "town" label. **Trade Yield retired (same day):** `effects-establish-trade-route` no longer queues `tradeYieldDelta`; `mods.tradeYield` had no reader (the two retired rows `reconstruction_drive` / `develop_infrastructure_std` still mention it — inert). **Trade/supply route edges (2026-09-07)** — `flags.bbttcc-territory.routes = [{hexUuid, kind:"trade"|"supply", since}]` on BOTH ends, written by `applyHexPendingSweep` from `turn.pending.routes` (queued by `effects-establish-trade-route/supply-line.enhancer.js` in bbttcc-raid when the planned entry carries `toHexUuid`). `deriveTradeRouteCountFromEdges(fid)` (turn-driver) is the FIRST route-count reader; **capacity is linear (owner ruling 2026-09-10): `tradeRouteCount × LOGI.CAPACITY_PER_TRADEROUTE` (0.5) — the old `floor(n/2)` pair rule gave a faction's first route nothing;** war-log "Trade Routes: N" and the nearest-six geometry guess are fallbacks only when a faction has no stored routes. Hex sheet shows a "Trade Routes" card when edges exist. Planner (`module.raid-planner.js`) adds a "→ To" select (owned hexes minus FROM) for `establish_trade_route` / `establish_supply_line`; `api.raid.planActivity({..., toHexUuid})` stores `toHexUuid/toHexName` in the planned war-log entry. **Plan-time target refusal (2026-09-08):** an effect may declare `canPlan({actor, targetDoc, targetFlags, activityKey, toHexUuid}) → {ok, reason}`; `planActivity` throws a `bbttccRefused` error the planner shows as a warning — the only refusal that costs no OP (cost is paid at turn resolution). First users: the six `found_site_*` effects (`effects-wilderness.enhancer.js`) refuse established towns (size ≥ town, a specific site type, or raised buildings) and point at Townbuilder → Found District. **OP economy ruling B (2026-09-11, sim `OP_ECONOMY_SIM_2026_09_11.md`):** `strategic-throughput.js PRICE_MULT = 0.75` scales EVERY strategic row's `cost`/`opCosts` once at attach (non-enumerable `__bbttccPriced` stamp on the cost object; retries on ready + 0.6/2/5/9 s catch enhancers that re-wrap defs with fresh literals) — source literals are BASE prices, `api.raid.PRICE_MULT` is the policy. `RECIPES` (`api.raid.RECIPES`, attached as `def.recipes[{label,cost,note}]`, scaled the same way) give establish_outpost / establish_trade_route / integration_framework (+optact) / develop_outpost_stability / upgrade_outpost_settlement 2–4 alternate FUELS across channels; the planner shows a ⚗ Fuel select when a row has ≥2, `planActivity({…, recipe})` stores `entry.recipe = {label,cost,note}` (summary "… via <label>"), and `turn-driver payActivityCost` bills `entry.recipe.cost` INSTEAD of the row price (dry preview too). **Culture + faith income lanes (owner ruling 2026-09-12):** `RES_TO_OP.culture {trade .3, knowledge .5, food .1}` and `.faith {knowledge .5, food .1}` in territory main.js (resourcesToOP now returns 8 channels); turn-driver `LANE_HEX_BONUS` adds per-type (culture: city 2 / research 1 / port 1 / settlement 1 / temple 1; faith: temple 3 / ruins 1), per-size (town 1 …) and per-stationed-facility bonuses (a stationary `rig` actor in `holdings.rigIds` whose NAME contains temple/shrine/chapel/church → faith 3/2; theatre/library/hall/festival → culture 2/1), summed into `computeTerritoryMatrixIncome`. Errata-shape ≈ culture 8, faith 4 per turn; a temple town ≈ faith 5. Sim parses both tables. **Price policy reach (2026-09-12):** `api.raid.PRICE_MULT` also prices travel legs (`hex-travel.js _applyPricePolicy` at ctx.cost creation — TERRAIN_TABLE stays in base marks; free passage / Ley Gate / costSet still apply after), siege muster (`siege-muster-pool quote`: troops × 1 mark × band mult × policy, min 1), and the raid staged-OP bonus (`module.raid-console stagedOpBonus`: +1 per 2 OP × policy = 15 marks). 🪤 A raid ROUND does not charge the 10-mark primary-pool gate — `_rcHasOpForActivity` is a threshold; what a round spends is its STAGED marks (+ maneuver costs). Courtly rounds are player bids in whole OP, not a price — unpriced by the policy. **Recipe side effects (ruling 2026-09-12):** each recipe may carry `fx = { faction:{moraleDelta,loyaltyDelta,darknessDelta}, hex:{loyaltyDelta,moraleDelta,darknessDelta,addModifiers,removeModifiers} }` and `days` (extra ledger days). `turn-driver applyRecipeSideEffects` runs when the activity resolves: faction tracks written directly (darkness = the `{global}` BOX), hex effects queued on the target's `turn.pending` and landed by `applyHexPendingSweep` the same Advance (sweep now maps `darknessDelta → mods.darkness`). Planner option text shows cost · fx · note; war-log summary appends the landed notes. **2026-09-09 "all six":** `strategic-throughput.js CAN_PLAN` map (`api.raid.CAN_PLAN`, attached to EFFECTS on ready + retries) gives ~30 target-sensitive rows plan-time refusals (unclaimed/held/rival/ally/spark/TO-hex). `_applyQuestEffects` verifies every faction's track after the write and retries once (unset+set) before erroring. Save restore seals invitation cards newer than the slot (`talkInvite.stale`, button reads "Predates the loaded save"). Faction sheet doctrine rows collapse duplicates to one row ×N. Tools: `audit-hex-arrivals` (arrival coverage per act, per hex), `dedupe-doctrine-items`, `audit-invite-lines`. **Tooltip prose authority (2026-09-08):** `module.raid-planner.js FALLBACK_TEXT` → published `api.raid.ACTIVITY_TEXT`; `ui/bbttcc-tooltip-resolver.js` reads it first (its own copy is now a fallback of the fallback). Option rows (`compat-bridge.js buildOptionStrategicDef`) carry `text`. Audit of all 120 rows + rulings: `STRATEGIC_ACTIVITIES_AUDIT_2026_09_08.md` (repo root, local doc) — 22 rows write flags nothing reads (`bonuses.nextTurn.*` has no consumer except `moraleBonus`), 4 pure no-ops, 10 duplicate sets; fixed: `dragon_s_parley` key alias, Psych-Ops now debuffs, Inquisition darkness sign. **WIRED 2026-09-09 (owner rulings 1–7):** every T2–T4 row in `strategic-throughput.js` now drives a real engine through adapters at the top of that file (`adjustFactionDarkness` = the `{global,[hexId]}` BOX — the old `adjustFactionTrack(A,"darkness")` overwrote it with a bare number and wiped per-hex keys; `unity` bumps route to `morale` = "Empathy Meter"; `setHexModifiers` immediate+ledger; `setHexDarkness` (`mods.darkness`); `setCondition` Purified; `revealHex`; `nearestNeighbors` 6-centroid; `addRegenPlan`/`addCapBump`/`setTruce`; `grantNextEnlightenment` from the pack; `rollCheck` via `rolls.flatCheck`). Handler ctx = `{factionId, attackerId, activityKey, targetUuid, targetName, toHexUuid, notes, entry}`. **Consumers** (`turn-driver.js`): `bonuses.regenPlan[{inTurns,mult,channels?}]` multiplies income in `advanceOPRegen` (inTurns ≤ 1 = this Advance); `bonuses.capBump{ch:{add,turns}}` lifts both cap clamps; `bonuses.truce{fid:{turns}}` + `bonuses.nextTurn.borderPatrol` refuse raids in `consumePlanned` BEFORE cost (`raidRefusalFor`); `nextTurn.spyInsertion{due}` whispers the rival's pools; `bonuses.nextTurn.opGainPct` (loyalty stability, 2026-09-10) multiplies all channels in `advanceOPRegen`; `tickFactionBonuses()` after regen is the only expiry (now also clears `nextTurn.opGainPct/defenseDC`) (`hardCleanupQueued` wipes `turn.pending`, so nothing may live there). `advance-turn.tracks.js`: Blessed Ground hexes −1 Darkness/turn, `nextTurn.noMoraleLoss` shields Darkness→Morale. `raid-roundflags` reads `bonuses.nextTurn.{initiativeAdv,freeManeuver}` and clears them. Siege: authored Sortie (threat-vectors) restored — counter-activities no longer registers `sortie`; `_defenderBufferManeuver` drains through `S.shaveBuffer` (buffer is an object; `Number({})` had made every drain 0). Planner: retired `propaganda_tour`, `reconstruction_drive`, `flaming_pitch`, `develop_infrastructure_std` (`RETIRED` map / `spec.retired`); `ROUTE_ACTIVITIES` += `smuggling_network`, `cultural_exchange`; 8 `found_district_<type>` rows (Town Growth, BU-only via `townbuilder.foundDistrict`). Scheduled OP that hits the cap is logged as lost (caps stay hard — owner ruling). **Option L2 rows (`optact_*`, 18):** all priced + wired 2026-09-09 (`OPTION_L2_SPECS`/`OPTION_L2_TEXT` in compat-bridge; handlers in strategic-throughput.js; `stewardSephirah()` resolves a steward's sephirah for Consecrated Alignment). **Maneuver audit 2026-09-09** (`RAID_MANEUVERS_AUDIT_2026_09_09.md`, local): `resolveRoundWithManeuvers` NEVER existed — the console's `_onCommit` is the resolver. Now: `_b3GenericPreRollMods` consumes authored `rollBonus/advantage/disadvantage` (window thisRound) BEFORE the dice via `agent.simulate.maneuver`; `bonuses.nextTurn.{initiativeAdv,freeManeuver}` consumed (`_b3ReadStrategicBoons`, cleared post-commit); scene verbs for pre-roll/post-commit maneuvers dispatched at commit through `sceneIntents.apply`; loader emits `bbttcc:raid:maneuversLoaded` → compat-bridge re-normalizes raidTypes/defenderAccess/availability + fire-mode tags re-run. `raid-roundflags.enhancer.js` is dead code (wraps a resolver that does not exist). Owner rulings 1–6 shipped 2026-09-09: `maneuvers-audit-wiring.enhancer.js` (pure bundle bodies for the no-ops + all 18 `opt_*` L1; `retired:true` rows skipped by `_effectsMans`; registers 4 passes incl. `bbttcc:raid:maneuversLoaded`), `_b3ApplyStateManeuvers` post-commit (Siege Breaker Volley / Engine of Absolution / Crown of Mercy / Ego Breaker), Saboteur's Edge + Bless the Fallen in the hard-coded switch, +40 authored maneuvers in the crew/occult/class grant maps, `OPTION_L1_SPECS` prices all 18, anytime budget honours `cost.anytime ≥ 2` as scope-exhausting. 🪤 Throughput handlers must stay PURE — the pre-roll consumer invokes them before the dice and B2 again at commit.

### UI / settings / tools
GM toolbar `#bbttcc-toolbar` (Dashboard/Create Hex/Overview + augment: World Map/Travel Console/Turn Driver); **Strategic Activity Ledger + Back Out (2026-09-08, `activity-ledger.enhancer.js`)** — wraps every `api.raid.EFFECTS[key].apply`; preUpdateDrawing/preUpdateActor capture BEFORE images during the apply window; changed top-level keys of `flags.bbttcc-territory` (per touched hex) + refundable `flags.bbttcc-factions.{opBank,buildUnits,opCaps}` are written to `flags.bbttcc-territory.activityLog[]` (cap 25) on every hex touched; **↩ Back out** (Hex Config → GM Overrides → Strategic Activity History) restores exactly those keys/refunds and stamps `backedOut`; API `api.territory.activityLedger.{list(doc), backOut(rec), wrapAll}`. Hex Config gained a GM **Scenes & Layers** tab (`data-ft-tab-panel="scenes"`) hosting the altitude/depth/battle-scene panels (they used to repeat under every tab). Territory Dashboard + Campaign Overview (**+ Faction relations rollup 2026-09-07** — every non-neutral pair via `api.factions.relations.list`, most extreme first, lopsided pairs shown both ways; tips `overview.relations/relQuiet`) + Hex Sheet (all AppV2, help-registered); Relationships Config; **Player HUD** (`player-hud.enhancer.js` — faction/steward/abilities tray, rig-aware emanation, only public `game.fourththing.*`); hex fog layer; click-to-edit. Settings: build-unit costs ×3, click-to-edit ×3, townbuilder costs + `townbuilderChargeMaterials` + `districtYieldShare`, fog ×4.
Tools (3) + macros (9): `assign-jackalope-holdings` [DRY_RUN=true], `darkness-stress-test` (writes by design), `link-hex-onenter-beats` [true]; `seed-hex-resource-nodes` ⚠ **DRY_RUN defaults FALSE**; `toggle-hex-visibility` = canonical fog reveal; quest-hint reveal/hide; audit-quest-hex-links.
⚠ NBSP hex names: only two defended sites (`main.js:797`, `townbuilder.js:71`) — anything else matching hex names by string is unguarded.

---

## 6. bbttcc-world

*World State Engine* v0.1.0 — canonical settings-backed world record `{turn, darkness, pressureMod, time{epoch,turnLength,progress}, locks, meta.snapshots}` + GM God Panel.

### API — `game.bbttcc.api.world` (`api.world.js:634-672`)
`getState/setState/applyGMEdit` · `bumpTurn/setDarkness/setPressureMod` · **Turn Ledger sink** `addTime/getTimeBudget` → `{ok, spent, remaining, debt}` · `getWorldLogs` · snapshots `list/create/rollback/delete/export/clearAll` · turn-beats `get/setTurnBeatsMap`, `get/setTurnBeats`. Emits `bbttcc:time:accrued` (`:628`) — its only hook.
GM God Panel via `registerMenu("bbttcc-world","gmGodPanel")` (restricted). All settings hidden. No tools. **SAVE GAMES (2026-09-08)** — `api.world.saves.{list, save({label,golden}), load(id,{deleteExtras,progress}), plan, readSlot, setGolden, remove, exportSlot, importFile, capture, dir, open}` (`api.save.js`): one JSON FILE per slot under `Data/bbttcc-saves/<worldId>/` (index in hidden world setting `saveIndex`); captures every world-scope Bad Eden setting + ALL actors with items/effects + ALL scenes' flags/drawings(hexes)/tokens + Bad Eden-flagged journals; EXACT restore (non-recursive updates, embedded replaced with kept ids, extra actors deleted opt-out, scenes never deleted) then reloads every client; ★ golden master slot. UI: `BBTTCCSaveGameApp` (AppV2, menu `saveGames`, 💾 **Saves** toolbar button, `save-game.hbs`). 🪤 No file-delete API — delete = stub overwrite + index drop. 🔴 competing spine with core (Appendix A #14).

---

## 7. bbttcc-structures

*Bad Eden: Structures* v0.1.0 (code actually Phase B–E) — material-BOM hardness model for rigs/facilities/fortifications: BOM → Plates/Threshold/resists; damage chips the BOM; states intact→damaged→breached→destroyed; collapse hurts tokens on top; recipes spend faction stockpile.

### API — `game.bbttcc.api.structures` (`api.structures.js:496-534`)
Pure: `computeFamily/deriveBOM/stateFromPlates/canonicalizeDamageType`. Mutating: `stampBOM/clearStructure/fullRepair`. `readState`. `crew.{capacity,read,assign,release,retypeFortification}`. Tables `FAMILIES/FAMILY_MAP/STATE_THRESHOLDS/RUBBLE_JITTER`. `recipes.{list,byId,canAfford,build,harden,repair,reload}` (`recipes.js:422`). `triggerCollapse/findTokensInsideFootprint` (`collapse.js:420`). `_wedge.{install,uninstall,isInstalled}` — the **registered damage interceptor** (converted from monkeypatch 2026-06-10, `damage-wedge.js:11-19`; overflow re-enters with `_skipInterceptors:true`). `bulwark.*` (Ruin-to-Renewal reclamation).
Emits `bbttcc:structure:{collapse, damageApplied, stateChanged}`. Sheet panel on Rig/Boss sheets (Harden/Repair buttons still stubbed → Appendix A #16). Data: `data/*.json`, spec `STRUCTURE_DAMAGE_SPEC.md`. Macros (5): build/damage/retro-stamp/sanity-check(read-only)/stamp-test-bom. ⚠ `damage-path.js` absent from manifest but live via ES import.

---

## 8. bbttcc-facility-console

*Facility Console* v1.0.0 — single-file hex-attached facility editor (bunker/tower/castle/custom presets: tier, SDR, OP modifiers, raid bonuses, travel effects, upgrades, per-turn effects, resolution hooks). Writes `hex.flags["bbttcc-territory"].facilities.primary` via mergeObject. **Pure editor — engines that consume its raw-JSON payloads live in territory.**
API surface = only `game.bbttcc.apps.FacilityConsole` (class; no `api.facilities.open`). Sole launcher: hex sheet (`bbttcc-hex-sheet.enhancer.js:1025`). No settings/hooks/tools. ⚠ authored `upgradesRaw/turnEffectsRaw/resolutionHooksRaw` JSON is unvalidated at edit time — malformed paste fails later inside the territory turn pipeline.

---

## 9. bbttcc-campaign

*The story engine* v1.0.1 — the **beat** as atomic authored content; three drivers (Story Director, dialogue surface via mal-voice, injector); world mutation, casualties, **Turn Ledger** (play→calendar days), **The Turn Press** digest, Campaign Builder (Flow Visualizer/Beat Editor/Table Editor/Quests).

### API — `game.bbttcc.api.campaign` (built `module.js:6845`, installed `:7578`)
- CRUD: `listCampaigns/getCampaign/saveCampaign/createCampaign/deleteCampaign/getAllCampaigns/setAllCampaigns`; `runCampaign`.
- **`runBeat(campaignId, beatId, ctx)`** (`:3884`) — canonical entry; applies `_beatEntryRedirect` (feud + `phaseEntry.belowPhase`); ⚠ deliberately ignores `inject.requires` ("gates are the Director's concern"). `executeBeat` (`:3043`) = the ~800-line lifecycle. **2026-08-27 lifecycle changes**: (a) encounter-type beats WITH choices now SHOW their dialog (old gate silently swallowed authored choices on any encounter beat w/ key); (b) a beat with its own `sceneId` whose registered scenario matches (`own.beatId===beat.id`) launches ITSELF, not the legacy core Encounter→Scenario map; (c) scenario launch is SKIPPED when the beat dialog handled the moment (own scene + dialog ran, or a choice routed on a successful check — failed check / fight pick / closed menu still launches); (d) scene-activation branch drops participating-faction PCs (`spawnFactionPCs`) when `_lastEncounterCtx.encounter.beatId` matches (covers dialog-type encounter beats like Acid Bog); (e) **failed choice checks re-offer the parent menu** (`_scheduleRetryOffer` — GLOBAL on-fail: failNext beat resolves, then the dialog re-opens; OP checks re-price each attempt; GM closes to let a failure stand).
- `gates.{report, requiresMet, value}` (`:6859`); `tikkun.get/max`. **Seals (2026-09-07→08):** `api.campaign.sealed(cid, beatId)`; `gates.report` leads with a `{kind:"seal", seal}` condition (gated+unmet, `out.sealed`) so consoles show 🔒 + a force override (Visualizer NEXT card, `data-force` → `runBeat(cid,id,{force:true})`); `runBeat` RETURNS executeBeat's `{ok, sealed, why}`. Act 0 exempt from both seals. **2026-09-09 owner veto:** the act seal closes repeatable hubs too ("walk beats are Act 1"); a routed child that is sealed ends the chain quietly (`{sealed:true, routed:true}`, no whisper). Per-act town hubs replace the exemption: `lyrenn_quest_scene` / `khezek_tor_quest_scene` / `allesh_gilliam_introduction_to_hq`; bbttcc-travel arrival reads a LIST (`hexOverrides[uuid].onEnterBeatIds` or `|`-separated ids; `readOnEnterBeatIds`) and falls through gated/sealed beats. Data patch: `tools/patch-act2-town-hubs.macro.js` (also: every `*acceptance*` beat now accepts its OWN quest — 10 accepted nothing, 2 the town quest — with a door into its investigation beat). Lint rules Q09/Q10/S01/S02/S03 in `lint-campaign.js` catch these classes going forward (acceptor-less gates, mis-accepting acceptance beats, down-ladder routes, ride choices without openTravel, single-act arrival).
- **`invites.{list, accept, playerAcceptAllowed}`** (2026-09-12) — open "wants a word" invitations as a surface: `list()` = directorState.invited minus answered (registry `word_<beatId>` exists / beat fired) with actor, line, where, act; `accept({beatId})` seals it from ANY seat (GM local, player → gmExec `bbttcc-campaign:invite.accept`, the beat's own speaker is the authority, confirmation names who accepted, chat card sealed too); hook `bbttcc:invite:accepted`; setting `invites.playerAccept`.
- **`director.{tick, chains, reconcileLevels, openCalendarDoors, state, addPressure}`** — `openCalendarDoors({turn, via})` opens every calendar hard door the given world turn has reached (idempotent; the turn driver calls it pre-regen with `cur+1`, 2026-09-12) (`:6874-6878`). Pressure accrual: turn +30, travel leg +8, raid round +10, resolved beat +3; threshold setting default 60. `speakerActorId` beats are **invited, not fired** (`:5017-5040`).
- **`dialogue.{choicesFor, enact, storyStateFor, doorsFor, pointTheWay}`** (`:6881-6885`) — the mal-voice contract.
- `placements.reconcile` (`:6887` → `reconcileNpcPlacements` `:6053`).
- **`ledger.{get, spend, remaining, timePointsForBeat}`** (`:6888`) — Turn Ledger.
- `tables.*` (+`runRandomTable` with real `dryRun`), `quests.*`, `io.*` (journal-pack bundle export/import/remap/stable-keys).
- `openBuilder/getActiveCampaignId/setActiveCampaignId`; post-build: `openBeatDialog()`, `audio.*` (BeatAudioManager), `stopBeatNarration`, `show/closePlayerFacingBeatDialog`, **`remirrorBeatDialog()`** (2026-08-29 — re-broadcasts the mirror for the GM's currently-open beat dialog; the player mirror is a SINGLETON, so overlapping chains/courier misses can eat it), `_lastEncounterCtx` (stash written by encounters).
- **`digest.{press, render, clear, journalName, opening}`** (`world-digest.js:277`) — The Turn Press. Headlines come ONLY from authored **`beat.digest`** strings of fired beats (max 6); no digest → never in the papers. Spares the authored Opening page.
- Also published here: `api.casualties.{applyFromBeat,applyToHex,applyToFaction,parseTags}` (`casualties-engine.js:542`), `api.worldMutation.{applyWorldEffects, pullTableToScene}` (`world-mutation-engine.js:1471`), `api.opSchedules.{list,tickFaction,tickAll}`, 🟡 `api.rolls.*` (zero consumers).

### Beat data model (as consumed)
- Identity: `id/label/description/type` (+legacy aliases). **Visibility:** `playerFacing` + 4 aliases (`playerFacingDialog/dialogPlayerFacing/playerFacingContent/showToPlayers`) OR-ed at read (`:2175`), normalized+written-back-to-all-five on editor save; broadcast = chat-courier mirror dialog. `beat.digest` (Turn Press headline), `beat.memoryText` (NPC memory; falls back to label+choice), `beat.inviteText`.
- Gates: `inject.requires` (AND-array; forms `{flag, gte|lte|eq}` over 15 named meters · `{questBucket, is|isNot}` · `{beatMark, quest, state}` · `{relation: <factionActorId>, is|atLeast: at_war…allied}` (2026-09-15 — the coalition's standing with a faction, either side's relations flag, higher wins; lint P07); fail-open on exception, unknown meter = unmet+warn)
- **WHERE (2026-09-15, owner ruling "story-based location awareness")**: `api.campaign.story.where(campaignId)` → `{hex, hexUuid, via, factionId}|null` (ONE reader over `api.travel.whereIs`: ride session → recorded arrival flag `bbttcc-factions.travel.atHexName` → the party's token on a hex map); `story.knownHexes()` → Set of `hexKey`s on every scene; `story.placeOf(beat)` → `"anywhere"` | hex name | null (rule: `beat.where` override → `beat.hexName` → on-arrival beats null → ambient/pool/Words (`_word_`, "Word from…")/rides (`_ride_`, "The Road…")/act openers = anywhere → else the quest's `QUEST_MAP.hex`); `story.hexKey(name)` (NBSP, band suffix `.c`/` c`, claimed-mark safe). `deriveSituation` takes `where` + `knownHexes` and stamps every `next`/road with `{place, here: true|false|null}`; NOW prefers a fresh next the party can reach (`why:"here"`), IN PLAY and DOORS sort here-first, `situation.where` echoes the hex. **executeBeat LOCATION guard**: a beat whose place is a known hex ≠ the party's hex is refused (`{ok:false, notHere:true, place, where}` + GM whisper naming the ride) — never on `ctx.force`, chains, or sources `phase-door|travel|hex_entry|travel-console-relay|dialogue`, and never when the position or the place is unknown. Rail/slate marks: 📍 here · 🐎 a ride away; the NOW card becomes "🐎 RIDE — …" with a run-anyway button. `bin/ft-replay-story` mirrors it (reads the save's arrival flag, plots the Travel Console when a pick is a hex away). Lint W01 = `beat.where` shape.. `inject.{repeatable, cooldownTurns, oncePerHex, oncePerFaction, oncePerHexGlobal, allowMulti, promptGM, fallbackOnDecline, enforcement, priority, storyChain, politicalTags, travel_threshold, debt_pressure}`. `phaseEntry{belowPhase,to}`.
- **PHASE D CLOSE-OUT (2026-09-17, STORY FLOW D-5/D-7/D-8 + the wall + the Obstructor)**: Harmonized Grove READERS — hex-travel `travelHex` (+2 on the travel check into a hex carrying "Harmonized Grove"; `ctx.harmonizedGrove`), raid-courtly influence (`createCourtlyScenario`: a side holding a Grove hex opens with +1 Influence); economy.constants `MOD_PRODUCTION["out-danced"] = −0.05` + `WORLD_MODIFIERS.out_danced` (the Cadence's border show). **RECEIPTS rename**: `api.raid.receipts` = alias of `courtlySecrets` (code identifiers unchanged); user-facing strings renamed (HUD "Receipts & Leverage", notifications, chat card title, Mal-voice "Extractable Receipts", onboarding tour). **"We haven't met"**: `beat.unmet = { beatId, html }` — the GM beat dialog leads with `html` when `beatId` is not in the story store (Pike / Tamsin / Etta's Act 2 openers). Content: `tools/patch-story-flow-phase-d2.macro.js` — the East Wall THREE-CHECK (Survey/Crew/Read → `ag_east_wall_*` ok/miss beats, `ag_east_wall_tally` GM-adjudicated, graded outcomes: success = Damaged Infrastructure lifted + Well-Maintained + morale/loyalty +1; `ag_east_wall_holds`; `ag_east_wall_patched`; failure = Damaged Infrastructure + morale −1), the three unmet lines, Out-Danced on `cadence_refuse` (lifted by the wins). **Obstructor Demon**: `packs/_source/npcs/Obstructor_Demon_LduYiOAoxyuDQzHV.json` rebuilt — five `power` items (Aura of Stagnation · Null Density · Multiattack · Wall of Nullity 2d10 psychic→Stress · Spell Scramble reaction) + the Slam weapon, AA flags, icons, Ghagiel manifestation blocks; duplicate entry `…vYVjgcHYwwYRM6J1` removed; `bbttcc-master-content/tools/patch-obstructor-powers.macro.js` swaps the WORLD actor's items in place (art kept).
- **PLAYTEST FIXES (2026-09-18, Act 1 tire-kick)**: hex-travel `_executeBeat` passes a run ctx (`source`/`trigger`/`injected: true`) so the LOCATION guard judges the arrival beat at the destination, and a refused injected beat (`sealed`/`notYet`/`gated`/`notHere`) reports `{ok:false, refused:true}` instead of counting as fired; `api.travel.whereIs` = end of the last EXECUTED leg, and a recorded arrival newer than the session wins; story-model NOW prefers a dormant quest's ready door HERE over the anchor's next elsewhere (`doorHere`); the Allesh-Gilliam Arrival script's Crossroads step stays current until Day's End (`done: {mark: "ag_days_end"}`) so the second ride is offered after the first. Chain routes carry the parent's `source`/`trigger`/`injected` (`_chainCtxFrom`), so a travel- or hex-entry-fired encounter's whole chain keeps the guard's arrival exemption; GM hub picks carry no source and stay guarded. (`ctx.__chain` is set only by bbttcc-encounters' trigger manager — the campaign module never sets it.) Content: `tools/patch-story-flow-playtest.macro.js` (DRY_RUN; sections accumulate) — P1: the Crossroads, Day's End and both rides are `where: "anywhere"` ("Call it a day" from another town routes into the hub instead of a location refusal); P2: all nineteen Tree's Session beats (`forest_of_tifaret_*`) are `where: "anywhere"` — a road encounter on the pinned Fixit leg, not a visit to PolygonForest.d. **Place audit rule**: a beat with no `where`/`hexName` inherits its quest's home hex; road encounters and hubs that are reached from other towns declare `where: "anywhere"` (chains are guarded like any GM pick — the parent's source is NOT carried, reverted 2026-09-18 after it fired Allesh-Gilliam's walk from Lyrenn). **Gate grammar**: `{ beatMark, not: true }` (store-backed) = met iff the beat has NOT been played (gate, report, lint, replay). **DAY'S END = THE MASTER TERMINAL (P3)**: every Act 1 town walk's "Call it a day" routes to `ag_days_end`, whose three choices are gated on the store — First Night unplayed → the day-one rail; First Night played → back to the Crossroads; a ride behind you → lock the turn (the ledger's three items live in that choice's description). `ag_ride_home` (The Road Home, openTravel Allesh-Gilliam) replaces the Crossroads' direct "Back to town"; Allesh-Gilliam's `hexOverrides.onEnterBeatIds` = [HQ intro (Act 2), `allesh_gilliam_town_walk` (Act 1, `oncePerHex: false`)] so riding home in Act 1 reopens the walk. The Crossroads text is generic (repeatable beats keep day-agnostic vocabulary — owner ruling). Replay: `ag_ride_home` in `RIDE_TO`/`ARRIVE`. **P4 — the HQ door knows the act**: `allesh_gilliam_hq_cinematics` (the town map's HQ door) is gated Act ≥ 1 with act-gated choices — "Find the Marshal" (Act 1 → the day-one welcome) / "Pike sent for you" (Act ≥ 2 → `allesh_gilliam_introduction_to_hq`, renamed "Pike Sends for You", opening rewritten as a summons after That One Night). Visualizer NOW card: the "(here)" suffix follows `next.here`, never the anchor's why ("(elsewhere)" dropped). **ROAD CHAIN BEFORE THE TOWN (2026-09-18)**: bbttcc-travel `_fireInjected` stamps `game.bbttcc._injectedChain = {beatId, trigger, startTs, settledTs}` around a road-fired beat's whole routed chain (socket `bbttccInjectedChain` mirrors it to every seat; hex_enter injections don't stamp); `api.travel maybeRunHexEnterBeatDeferred` treats an in-flight chain as an encounter and its settlement lane waits for the chain to unwind plus 3 s of quiet (no Bad Eden modal incl. `.bbttcc-campaign-dialog`, no combat; hard stop 45 min) before the destination's on-enter beat fires — the Tree's Session finishes before Fixit's intro opens. **CHAPTER SEAL (2026-09-18)**: `sealOfDecl` seals a chapter's `start`/`ending` beats once the chapter has an ending (kind `chapter`); other beats declared in the chapter stay open. **P5 (Fixit)**: the Weeping Prisoner's five beats point at `arc_bay_back_room_pov`; the Arc Bay's crying-Fey question and "get Mara" offer hide once any ending played (four `{beatMark, not}` rows); Mara's "Trade" / "Shared Oversight" pitches carry `cooldownTurns: 1` (a failed pitch waits a turn; Hard Ask and Delay remain). **P6**: the Gentle Pest's "Teach a Local" ending just exits ("Leave" → quest scene; the stray second Persuasion check and "Not today" removed). **AUDIO — latest-only behind the lock (2026-09-18)**: `beat-audio-manager._installLockedLatestOnly` wraps the static `AudioHelper.play` receiver — while `game.audio.locked` (no gesture yet on that seat) a broadcast one-shot is parked, a newer one replaces it, and only the survivor plays when `game.audio.unlock` resolves (Foundry's `Sound#load` otherwise awaits the unlock per sound and every queued narration blasts on the first click). Looping sounds and pushes pass through. **OUTCOMES APPLY ON OPEN (2026-09-18)**: an `outcome_trigger` beat applies its world + quest effects when it opens (before its dialog; `_applyBeatEffectsOnce` at `open`), so a card-producing outcome (the Water Choir's `hexReading`) shows without a pick; dialog / skill_scene beats keep pick-time effects. **GM choice buttons** carry the check they roll (" · Perception DC 12"). **P7**: the Water Choir's three inspects are named by their stat. **P8**: the Harmonized ending carries `hexModifiers: [{add:["Harmonized Grove"], hexName:"PolygonForest.d"}]` (editor World Modifier rows land on `tf.worldModifiers`, a separate display list — the Grove READERS key on the named `tf.modifiers`), plus a live retro-repair when the store says Tifaret closed harmoniously and the hex lacks the Grove. **P9 — THE CULT, IN ORDER (2026-09-19)**: the Lift Hall offers "Ask Sable Nine to take you down" → the Seal's start (hidden once started); the Seal's acceptance opens with Sable leading; `vs_bridge_seal` ("The Seal Was a Signature") is THE REALIZATION — placeless, player-facing, requires a Seal ending (anyOf) + Etta's exit, routes to the Upper Galleries; `khezek_upper_galleries` is `hardGate: true` on `{beatMark: vs_bridge_seal}` (hub doors included). `story-scripts.js` gains a light `valhaulan_spine` script (word · signature · triangle · corroboration · muster) so NOW stops stalling on the dead overture; lint SC05 ignores borrowing steps. **P10 — Pilgrim Wick enters**: `ag_vacancy_pilgrim_wick` + `_routes` (Act ≥1 Vacancy door beats, speaker = the Wick actor, scene `the_vacancy_interior`; the Vacancy intro gains "The guest in the corner"); `kt_courier_has_a_face` (Act 2, placeless, after the Galleries' "Withdraw quietly"; recognition branches on `{beatMark: ag_vacancy_pilgrim_wick}` / `not`; grants the Receipt "The Courier's Route"). **P11 — ACT 3 WAITS FOR ACT 2 (owner ruling 2026-09-19)**: the three Seal endings lose `phaseAdvance`; `a3_title_card` (spine, placeless, `phaseAdvance {set:3}`) waits on `allesh_gilliam_pike_closure` + `lyrenn_hex_settles` + `khezek_hex_settles` + `vs_bridge_seal`; the calendar door (turn 6) remains the safety valve; repair sets a world back to Act 2 when Act 3 opened early and no Act-3 beat played. **THE SPINE SEALS, THE ASIDES STAY**: for a scripted quest the quest-closed seal applies to step beats, the start, closers and the quest's own endings only — doors, chapters and other asides stay open after the close (Etta's Long Market conversation survives Pike updating the map). Replay: opens the door a gate names (`beatMark X` unplayed and openable, from NOW or any IN PLAY row) before calling the turn. **MODEL (2026-09-20)**: `deriveSituation` does the same live — when NOW would say turn/complete/empty and a quest IN PLAY waits on a `beatMark` the party can open now, NOW = that beat (or its unplayed entry in the same quest — Etta's conversation, not her exit), `why: "gate-door"`, `next.opens` = the waiting beat, `next.named` = the gate's beat, `next.line` = the waiting step's line. **`_beatGateReport` understands `{anyOf}`** (2026-09-20 — the Visualizer showed the realization BLOCKED while the gate itself passed; readiness comes from the report). ⚠ Acts 3+ still self-advance (`map_crown_mall_intro` sets Act 4 on the spot) — curation owed. **P12 — DAY'S END, ACT 2 ONWARD**: `days_end_thatwards` (placeless, evergreen, repeatable, generic text; "Run it" + "Daylight left") reached from every Act 2 hub's "Call it a day" (AG summons hub, Lyrenn second season, KT quest scene, Fixit intro + walk) — a town-day per turn.
- **ACTS 3–6 SCRIPTED (owner rulings A–M, 2026-09-20)**: `story-scripts.js` gains twelve scripts (widening_trail with six chapters incl. the new `the_burnt_flats`, hidden_vault, balcones, flooded_towns, lost_statues, fifteen_year_siege, trojan_gift, crown_mall, maneuver_vault, ninth_guest, finale, gloomgill); QUEST_MAP: `the_burnt_flats` chapter (`quest_burnt_flats`), quest hexes finale = Inconvenient Mountains.h, ninth_guest = Founder's Garden, balcones = Saltwake Reach j, fifteen_year_siege = Perspicacity Fortress, maneuver_vault = Hexen Myre.c. **Act doors**: 3 = `a3_title_card`; 4 = `map_crown_mall_intro` (waits on Legansus; the mall's entrance beats are Act ≥ 3); 5 = `vs_bridge_muster`; 6 = `a6_title_card` (any finale ending). **`beat.storeFacts`** (ruling M): the GM dialog leads with a "📒 What the story knows" ledger — closed quests + endings, chapter endings, open threads, Receipts held (`api.raid.courtlySecrets.getSecrets`), militia rungs, the meter settings — GM only, never broadcast (Gloomgill's ten questions). **Seeder** `tools/seed-story-flow-acts-3-6.macro.js` (dry run, then a confirm dialog to apply — no constant to flip): S1–S13 per the worksheet's punch list; every site gets `hexName` + a `hexOverrides.onEnterBeatIds` opener; the Hidden Vault and the Mall come off the travel pool; the Trail's sites gate on the previous site's ending and lose the extraction's `alsoEnds`; the Mall closes at its terms (the archive is only the door); S14 binds scenes by NAME (the three Hex Flooded Town battlemaps; the Mall: Donny → food court 1 → food court 2 for the archive → the halfpipe / Bev's office / Miss June's store). Golden replay: from the top through Gloomgill with 0 refusals, every act by its story door.
- **STORY-DRIVEN SPARKS NEVER FAIL ON REACH (2026-09-20)**: bbttcc-tikkun `hex.integrate(uuid, { story: true })` debits the faction's Reach when there is Reach and proceeds regardless; the world-mutation engine's `hexSpark` handler passes `story: true` (a beat's integration is its own Act of Repair). Live-caught: the Valhaulan Seal restored, "integrate refused at Khezek-Tor: reach exhausted". Playtest macro P13 integrates KT's seated spark after the fact when the store says the Seal was restored. **Effects audit**: `~/EFFECTS_WORKSHEET_ACT2_2026_09_20.md` (local) — 65 Act 2 endings, 34 wired / 31 prose-only, proposals per ending, rulings U (marks vs OP in beat text), R (relationship targets need faction actors), T (timed states = worldModifiers with durationTurns).
- **RECEIPTS + THE TOWN MILITIA (2026-09-17, STORY FLOW Phase D-3/D-4)**: `worldEffects.receipts = [{label, effectKey, truth?, acquisition?: earned|stolen, source?:{name,npcActorId}, factionId?}]` (world-mutation-engine 2h) grants a Receipt Item (the courtly-secret item family — owner ruling: "Receipts") to the coalition faction via `api.raid.courtlySecrets.addSecret`, cloned from `bbttcc-master-content.courtly-secrets` like Mal-voice's `_grantSecret`; dedupes by name per faction; chat card; lint E15. `worldEffects.militia = {rung}` (WME 2i) raises the Town Militia's rung. **`api.raid.militia.{state(actor)→{rung,drills,hexes,absorbedTurn}, set(actor,patch), founded(actor), HOME_HEXES}`** (strategic-throughput.js): rung 1 FOUNDED = `quest_ag_town_militia` active/completed on the faction's quest track; rung 2 DRILLED / rung 3 STANDING via the new **Muster Drill [T1]** activity (`data/bbttcc_activities_v1_4.json`, violence 20 base; planner group Hold & Defend). Readers: raid-console `_rcDefenderNextBonus` (+1 founded / +2 drilled+), turn-driver `advanceOPRegen` (+5 marks Violence while drilled+), advance-turn.tracks `doLoyaltyPhase2` (STANDING absorbs the first unrest/infra strike on a home hex per Advance, `militia.absorbedTurn`), hex-travel `passageFor` (STANDING → home roads free, why "militia escort"). First cut: the defense bonus applies to any defense by the militia's faction (its holdings are the home hexes in Act 2); the escort is "home roads free while Standing" rather than one caravan per turn.
- **CHOICE GATES + COOLDOWNS + PINNED LEGS (2026-09-17, STORY FLOW Phase D-1/D-2/D-9)**: a choice may carry `requires: [cond…]` (the beat gate grammar) and `cooldownTurns: N` (measured against the director's choice ledger by beat id + label); hidden choices are not rendered (GM beat dialog, player mirror, `dialogueChoicesFor`) and `_enactChoiceCore` refuses them (`_choiceHidden` / `_visibleChoiceIndices`; the ledger keeps ORIGINAL indices). Gate grammar gains `{ anyOf: [cond…] }` (OR inside the AND list; recursive). `beat.pinLeg = { to?: hexName, from?: hexName }` (bbttcc-travel `maybeInject`, travel_threshold): the beat wins the leg's draw ahead of the pool and the global cooldown when the endpoints match (its own gates + once rules apply; `thresholdCtx.hexName / fromHexName` added; fire path shared as `_fireInjected`). Lint: **CG01–CG03** (choice gate refs · cooldown shape · pinLeg shape/repeatable), anyOf members validated. Replay mirrors anyOf, pinned legs (📌) and the `crVerify` tally. Content: `tools/patch-story-flow-phase-d.macro.js` (make camp waits for a ride; the two forests' treaty choices wait for the other's peace; Etta's Follow-up holds a turn; the Riders pinned to the first Lyrenn leg at turn ≥ 2; Tifaret pinned to the Fixit ride).
- **SCRIPTS (2026-09-17, STORY FLOW encode Phase A/B)**: `api.campaign.story.{QUEST_SCRIPTS, scriptOf(key), registerScripts(map), scriptView(script, ctx)}` — a scripted quest DECLARES its order (`scripts/story-scripts.js`: giver · player-facing description · ordered steps `{id,label,beats[],line,hereLine?,group?,chapter?,done?,handoff?,borrow?}` · doors · after · arrival{act:1,steps} · chapters{key:{giver,line}}); for a scripted quest `deriveSituation` sets `next` = first unfinished step whose gate is met (step-scoped frontier after the entry beat plays; `group` = any order; `done` = anyOf/allOf/mark/chapter/quest; `handoff` resolves to the target quest's next; Arrival steps seal at phase 2; closes ONLY by a declared closer) and attaches `q.script` (steps with status, current, doors, next.line). Unscripted quests keep the FRONTIER/LADDER inference byte-for-byte. Renders: slate + NOW card + Visualizer hero/IN PLAY (`next.line`), Quest Log rows (giver / description / Next lines / step label via `api.campaign.director.situation()`). Tests: `tools/story-model.test.mjs` (`node … [save.json]`). Lint: `lint-campaign.js` reads SAVE SLOTS (`kind: bbttcc-savegame`) and adds **SC01–SC06** (ids exist · cross-quest steps · lines · chapter/quest refs · one owner per beat · closer reached by the last step). Replay: Act-1 arrival at Allesh-Gilliam = the Welcome Round.
- Director: `storyChain` (chain order = beats-array order), `priority high|normal|background`, **`speakerActorId`** (NPC embodiment → dialogue surface; `dialogueOffer:false` opts out), director-excluded when `targetHexUuid` / tag `discovery` / `timeScale==="leg"`.
- Content: `sceneId/sceneUuid`, `journal` (GM auto-open), `actors[]`, `audio`, `refs`, `cinematic{enabled,startSceneId,nextSceneId}`.
- Choices: `label/description/prompt/style`, **`next`/`failNext`**, checks (`checkStat/checkDC/checkMode/checkPrompt`), OP support (`support/supportSpend/supportOpKey`, desperation gate, HP-sacrifice), `politicalTags`. **Check semantics (owner ruling 2026-08-30, enforced in dialog + dialogueEnact paths): op.\* checks AUTO-ROLL and gate/spend/roll against the SELECTED steward's faction (`_factionForRosterActor`, primary-faction fallback); ALL non-OP (aptitude) checks go to GM pass/fail adjudication — authored `checkMode:"auto"` is ignored for them.** The dialog's Roster Member dropdown lists ALL participating stewards (campaign coalition rosters, `actorKind==="steward"` only), and the summary OP pool/bonus chips render one labeled block per coalition faction.
- Effects `worldEffects`: `factionEffects/relationshipEffects(rows {sourceFactionId, targetFactionId, setStatus|step, reason}; either side may be `"@coalition"` = the active campaign's roster, 2026-09-15)/worldModifiers/turnRequests/questEffects/warLog/radiationDelta/territoryOutcome/purifyHexes/openTravel(prefills Travel Console)/phaseAdvance/tikkunDelta/levelEffects(monotonic floors)/politics`. Plus `unlocks{maneuvers,strategics}`, `gateUnlocks`, `questId/questStep/questRole`, `offerQuest`, `sparkLink` (Tikkun), `handoff`, `encounter{key,tier}`, **`chase`** (travel seam), `timeScale/timePoints/turnNumber/chapter/act/tags`.

### Hooks
**Seals (2026-09-07)** — `_beatSealed(beat, campaign, ctx)` is the one authority for "completed quest ⇒ its beats never fire" and "finished act ⇒ content done in its entirety" (act = the `gte` of the beat's storyPhase gate; act 0 exempt; `inject.evergreen:true` opts out; `ctx.force:true` overrides one run). Enforced at `executeBeat` ENTRY (every path incl. runBeat chains, chat buttons, hex-arrival injector) and inside `_beatRequiresMet` (Director, conversation offers, invite cards). Settings `director.sealCompletedQuests` / `director.sealDoneActs` (world, default on). Query: `api.campaign.sealed(campaignId, beatId)`. Refusals whisper the GM a 🔒 card.

Emits **`bbttcc:beat:resolved` `{campaign, beat, ctx, outcome}`** (`:3748`, the cross-module spine) + `bbttcc:dialogue:choiceEnacted` + `bbttcc-campaign:updateBeat`. Listens: 6× `bbttcc:advanceTurn:end` (director/ledger/title-card/announce/press/op-schedules — all GM-gated EXCEPT 🔴 ledger phase-door, Appendix A #11), `bbttcc:time:accrued`, `bbttcc:afterTravel` (ledger debit + director seam), `bbttcc:raid:roundCommit`, 10× `bbttcc:beat:resolved` meter subscribers, `fourththing.overshoot` (Reality Tear → adversary beat, primary-GM-elected + socket relay).

### Settings / UI / tools
**Beat lint (Phase 4, 2026-08-29)**: GM ready +8s sweeps the active campaign (broken next/failNext links · all-checked no-exit menus · hub-reachable zero-choice venues); toasts only when dirty, one console line when clean. Also 2026-08-29: fired-history `ts` refreshes on EVERY fire (hero anchor follows the table's present; `firstTs` preserved), and the beat-dialog close-race is fixed (`buttonTaken` — a clicked button's callback owns the promise; teardown never resolves early). bbttcc-factions: **token faction sheets redirect to the world actor** (`BBTTCCFactionSheet._render`).
Stores (hidden): `campaigns/injectState/encounterTables(read cross-module by encounters)/quests/activeCampaignId/lastTurnAnnounced/directorState/ledgerEntries` + 15 named meters. **`directorState.choices[]` (2026-09-07)** — append-only choice ledger (cap 200) written in `executeBeat` right after the beat dialog resolves: `{v, ts, turn, campaignId, beatId, beatLabel, questId, choiceIndex, label, next, checkOk}`; the plain "what did the table pick" (Decision Memory only records politically tagged picks). **Quest schema** gained `completedTs`/`completedTurn` — stamped once by `setQuestStatus("completed")`, cleared on reopen/archive; `_normalizeQuest` preserves them. Dials (visible): `ledger.turnTimeBudget`, `ledger.travelDaysPerTier` (CSV `1,1,2,3`), `overshoot.drawsBeats`, `director.enabled/pressureThreshold(60)/autoInvite/turnTickOnlyChains("valhaulan_spine")`, `audio.debug`.
UI: Campaign Builder (AppV1, ~4,700 lines; **Flow Visualizer rebuilt 2026-09-07 — ONE quest at a time, top-down tree, follows the quest of the last fired spine beat (`user.flags.bbttcc-campaign.flowFollow`), dashed → exit stubs jump quests, click-a-beat card with Run/Edit; rail = NOW · RECENT · LOCKED only; Director chains fold into NOW as per-quest progress (CHAIN rows for chains with beats in the charted quest); quest dropdown shows fired/total — quests-overview/lanes views, Turn/Act/Scope filters, pressures/browse/coming-up rails RETIRED from this surface; Faction relations MOVED to the Campaign Overview (bbttcc-territory); Director pressure shown as a `⚡ n/threshold` chip in the truth strip (hot at threshold); arc flags (wendigo/bandit/cadence/grief/geburah) deliberately have NO dashboard — the beat card names them in context (owner ruling 2026-09-08)**), Beat Editor (3,793), Table Editor (`apps/` copy is live), Player Beat Mirror, toolbar 🎬 button, 8 chat-card families (quest offer, title card, talk invite, handoff, level-up, tier, Wendigo/Bandit rungs).
Tools: **85 macros** — 48 DRY_RUN=true (incl. 2026-09-15 `patch-vault-allies` — Vault rescue closers set coalition↔Jackalopes ALLIED, Back Stairs/Generator hard-gated on it, Siege Week chapter removed, Touring Gift ending "kept", Hum registry row; step 12 of `run-golden-ritual` — 2026-08-27 `patch-bandit-ambush-mercy` — wires win-beat kill/jail/free choices into the mercy-counted outcome beats, ✅ applied — `seed-words-doors` — parley/commune door + success/fail beats on 10 travel encounter families, tags `words_door`, esoteric-shelf checks meditation/insight/occult/ritual, ✅ applied — and 2026-08-28 `patch-steel-doors` — no-check FIGHT choice beside every words-door, hub-routed when a neutral Win/Lose/Run outcomes hub exists; fixes the fail→re-offer loop trap — ✅ applied; and `patch-no-exit-menus` — unchecked exits for the 6 all-checked menus the link audit found: hazards get slow-lane beats costing timePoints 1, Gilbert routes to his fight, social menus walk free; + bandit steel door → live hub `bandit_ambush_outcome_results`), **19 DRY_RUN=false (write on paste)**, 18 read-only/tests (new: `audit-beat-links` — campaign-wide broken next/failNext/outcomes refs + NO-EXIT MENUS where every choice is checked + words-door family link map). **`patch-leyline-stabilizer-gate` (2026-09-10, DRY_RUN)** — adds `{questBucket: quest_bSwOIWzxqNBwJ5NM, is:"active"}` to the Leyline Stabilizer START beat `fixit_leyline_stabilizer` so the chain stays dormant until Yarrow's HQ beat accepts the quest (it sat READY at Fixit Farm on storyPhase ≥ 2 alone); reports the beat's sceneId resolution. 🪤 beatMark gates only open for beats whose questEffects carry a `beatId` — nothing writes a generic "seen" mark on fire. **`seed-a0-b9-adam-kadmon` (2026-09-07, DRY_RUN)** — inserts Act 0 beat A0-B9 "Guess What" (Adam Kadmon VO, step 75 between Wake-up and Teaching Slide 1) and points `bbttcc-onboarding.campaignWakeBeatId` at it. **`patch-act2-punchlist` (2026-09-06, DRY_RUN)** — fixes every lint ERROR on the Act 2 bundle (op.cult, vs_overture bucket, empty ashwastes table) + 12 fail-route checks (proposals) + 22 duplicated factionEffects rows; simulated 5 ERROR → 0. **`walk-act-headless` (2026-09-05, GM, sandboxed)** — fires every beat of one act through the real runBeat pipeline in a throwaway campaign with temp factions, asserts effects/quests/phase/routing per choice (forced pass + fail), stubs live-touching APIs, restores everything; pairs with the offline `tools/lint-campaign.js` (node CLI; `bin/ft-lint-campaign` symlinks to it) — see `ACT2_LINT_2026_09_05.md`. Read-only highlights: `audit-beat-reachability`, `audit-beat-actors`, `audit-monodynamic-exposure`, `campaign-census`, `press-digest`, `reset-console` (🔄 Bad Eden Reset Console). See the module's tools dir; ship-hot list in the survey of 2026-08-27. **2026-09-07: default wake beat → `fates_and_destinies_adam_kadmon`; new setting `wakeThemeSrc` (default Section D) — the wake handler plays it to EVERY client via `AudioHelper.play(…, true)` when the wake beat resolves.**

---

## 10. bbttcc-travel

*The overworld layer* v1.4.0 — hex-to-hex movement (OP cost, terrain tier, weather, domain gating, 2d10 travel check + encounter roll), multi-leg **Travel Console** with passenger factions + armed mitigation, vertical dive/ascend, scene transitions, world overview map, and the **Chase** primitive. Hosts the LIVE CampaignBeatInjector.

### API
- **`api.travel.passageFor(factionId, hexLike)`** (`hex-travel.js`, 2026-09-07) → `{free, why:"owner"|"allied"|null, ownerId, devStage}` — THE free-passage authority (dev-6 hex + owner-or-ally ⇒ travel cost 0). Used by `travelHex` execution AND the Travel Console (leg rows, totals, stack estimate, passenger debits). Any new cost preview must call it; before this the console showed full freight while execution charged 0.
- **`api.travel.travelHex`** (`api.travel.js:1008`; core engine `hex-travel.js`, drift guard re-asserts the wrapper `api.travel.js:1016-1036`).
- `travel.domains.*` (`hex-travel.js:1807-1824`): terrainMedium, requiredDomainsForTerrain, rigDomains, factionTravelDomains, canFactionEnterTerrain, factionSubDepth, factionAirReach.
- `travel.dive/surface/ascend/descend` (`:1827-1832`); `api._hexTravel.{TERRAIN_TABLE, getHexAtPoint, getHexTerrainSpec, _encounterDc}` (the console's preview contract).
- `travel.emitAfterTravelWithEncounter(...)` (`bbttcc-travel-console.js:2765`).
- `travel.weather.{archetypes, get, tooltip}`; `travel.arc.rollStep`; `travel.mitigation.{rosterAbilities, coverageFor, coversWeather, filterByWeather, usesFor, getMode, setMode}`.
- **`travel.chase.{start, runLeg, end, open, state}`** (`chase.js:601`). Lead-meter pursuit: lead≤0 CAUGHT, ≥escapeAt ESCAPED; per-leg Pace `2d10+tier+speed*2+bonus`; terrain bias `hazard/combat/discovery/extreme/mix`; domain gates as tactics (pursuer can't enter → quarry escapes; quarry can't → cornered −2); gambits `redline/potshot/feint` at 10 marks via `op.commit`; `endChase` fires `bbttcc:chase:resolved`, war-logs both sides, then runs `followUp.{onCaught,onEscaped}` via `campaign.runBeat`. **Beat seam**: author `beat.chase = {quarry, pursuer, route[], lead, escapeAt, onCaught, onEscaped}`; side spec accepts `{fromCtx:true}`. State survives reload (`bbttcc-travel.activeChase`). GM Chase Console 🏁.
- ⭐ **`api.travel.rideSession.{get, list, save, clear}`** (Phase 2, 2026-08-29) — the ride as a first-class persisted object: world setting `bbttcc-travel.rideSessions` keyed by factionId `{id, factionId, joiningFactionIds, legs[], executed, stage riding|encounter, encounter{beatId,key,label}, createdTs, updatedTs}`. Console writes at execute-start / encounter-pause (remaining legs; final-leg pause clears — arrival owns it) / completion (clears); player-seat writes relay via gmExec `travel.rideSession.save`. Console shows **"↻ Resume ride — N legs remain"** when a session exists and the planner is empty; Resume restores legs. Reload mid-ride no longer loses the route.
- `api.transition.{dive, back, flash, shake, portalSwirl, requestDive, consumeDive}` (`scene-transition.js:294`) — road encounters use `audience:"activate"` (2026-08-26 ruling: table moment).
- ⭐ **`api.travel.doors.{list, forQuest, beatsForQuest, litDoorsHere, dive, goto, pulse, refreshLit, isTownHub}`** (`location-doors.js`, 2026-09-14) — **TOWN HUB DOORS**: a composited settlement map (Dungeon Alchemist `~/bin/da-merge` output) whose buildings are clickable. Scene flag `bbttcc-travel.isTownHub` + `townLabel` + `hexUuid`; door Drawing flag `bbttcc-travel.locationLink {key,label,targetSceneUuid, sceneUuids?[] (every scene that IS this location — exterior/interior POV + battlemap — for quest matching), beatId? (opener beat: a click RUNS it via `campaign.runBeat` after `transition.requestDive`, so the beat's sceneId + chain carry the table inside; sealed/failed → plain dive)}`; location scene carries the existing `returnLink` → "⬅ Back". Plain left-click (stage pointerdown hit-test, ellipse-aware) → `transition.dive(..., audience:"activate")` on a GM seat; a player click relays via gmExec `travel.doors.dive` (world setting `doorsPlayerActivate`, default on; refused unless the table's active scene IS that hub) else a local view-dive. **Quest seam = `beat.sceneId`** (no new schema): doors whose target scene is named by an un-completed beat of an active quest PULSE gold on the hub; `forQuest(questId)` feeds the Quest Log's 📍 pills; `goto(drawingUuid)` pans this client to the door (view-dives to the hub first if elsewhere) and pulses it. Hex Drawing flag `bbttcc-travel.townHubSceneUuid` points a hex at its hub. **Setup = ONE runner + a towns config (2026-09-17): `tools/town-hubs/setup-town-hub.macro.js` (set `TOWN_KEY` + `DRY_RUN`, paste as a Script macro; it fetches `tools/town-hubs/towns.json` from the module) — flags hubs, places doors + cross-town bars at render-pixel coords scaled to the import, seeds/SYNCs exterior cinematics (tag `town-door`), writes returnLinks + hex `townHubSceneUuid`, fills empty/dangling beat `sceneId` / `cinematic.startSceneId` / `nextSceneId` (never a live id unless `overrideLiveBeats` / `replaceSceneNames`), `removeBeats`/`removeDoors`/`leave`/`notes`. Towns wired: fixit, ag (South+North), lyrenn (Ring+Rows), kt (one crater map). A new town = a data block in towns.json.
- `api.worldMap.*` (`world-overview.js:465`), `api.travelPlanner.{open,close}` (planner UI pill deliberately killed — API alive, no entry point), `game.bbttcc.ui.travelConsole` singleton, `game.bbttcc.runVisuals`, overlay renderer globals.
- **`game.bbttcc.api.campaigns.injector`** (`hex-travel.js:1588`) — THE canonical injector (ruling 2026-08-28): `maybeRunBeatById` (hex_enter, `defaults:{oncePerHex:true}`; **since 2026-09-08 also gated by `inject.requires` + seals via `_requiresMet`** — the by-id path used to skip the story gate). **`api.travel.whereIs(factionId)` → `{hexUuid, doc, name, via:"ride"|"token"}` | null** (paused ride's first remaining leg, else the hex under a faction steward's token on the `isWorldHub` scene / controlled token); **`api.travel.arriveAt(hexUuid, {factionId})`** fires the per-act on-enter list. `worldMutation` `openTravel` uses both: destination == current hex → arrive, return `alreadyHere`; `executeBeat` then skips the ride dialog (2026-09-09)., `maybeInject("travel_threshold")`, `dryRun`. Tag matching: ctx→tags `trigger./terrain./band./radiation./tone.` **+ autoDebt tags** (`debt:<n>`, `debt` at ≥2 `[HV_DEBT:` war-log markers); hard-prefix filter + overlap score **+ foreshadow.<chain> +15 when the chain is in motion**; `inject.requires` gated via `api.campaign.gates.requiresMet` (fail-open); debt-ish winners need a GM Run/Decline (decline persists per campaign:hex:beat in `state.declinedDebt`, falls through; non-GM clients skip debt beats); no injection in combat; 1-turn global cooldown (hex_enter exempt).

### Travel math & OP debits
Check: `travelAdvantage ? 3d10kh2 : 2d10` `+ @int + @scout` (`hex-travel.js:1131`); encounter DC = base 12 + tier×2 (settings `travelEncounterBaseDC/TierStep/travelDomainGate`, world-visible). Lead faction pays via `spendOP → op.commit` inside travelHex (`:528-553`); **passenger factions each pay their own leg debit** (Phase E1, console `:2150-2230`, allied-only join); mitigation bridge stages/consumes uses on `beforeTravel`/`afterTravel` (forecast mode never drains). Console: Vanguard Stack armed-mitigation, `worldEffects.openTravel` prefill seam (`:2681`), floating "Open Travel Console" button.

### Hooks
Emits `bbttcc:beforeTravel`, `bbttcc:afterTravel {…, success, rollTotal, dc, encounter, joiningFactionIds, participantFactionIds}` (participant fields added 2026-08-27 — feed the encounter-scene PC drop), `bbttcc:chase:resolved`. Listens `bbttcc:advanceTurn:end` (weather tick, GM-gated), `bbttcc:beat:resolved` (chase seam, GM-gated), `updateToken` (hex arrival). Socket `module.bbttcc-travel` encounter arbitration (primary-GM elected).

### Tools
13 macros: `start-chase`, `dive-here`, `setup-underwater-dive`, travel gauntlet forge/runner/static-audit (audit read-only), overlay helpers, world-overview setup ×5 (`world-overview-migrate` is the only DRY_RUN-gated one, default true). ⚠ 12 of 13 write on paste.

---

## 11. bbttcc-encounters

*Arbitration + staging* v1.0.0 between a travel result and a playable scene. Listens `bbttcc:afterTravel` (GM-gated first statement), Launch/Decline/**Reroll** policy dialog (reroll re-picks from campaign travel tables `travel_<terrain>_t<tier>` → `travel_generic_t<tier>`), then: **preferred path** = stash ctx on `campaign._lastEncounterCtx` → `transition.requestDive({audience:"activate"})` → `campaign.runBeat(campaignId, beatId)`; **legacy path** = static Encounter→Scenario→Scene registry (on retirement path — warns on every use).
**Spark faucet CAPPED (Descent A2, 2026-09-03)**: all 5 `gatherSpark` sites in `scene.launcher.js` route through `gatherSparkOnce` — one grant per spark key per actor ever (reads `flags["bbttcc-tikkun"].sparks`); the 4 ad-hoc keys (splintered_hod / minor_fallout / qliphotic_vault / shattered_chorus) no longer mint repeatably.

### API — `game.bbttcc.api.encounters` (`api.encounters.js:1076`)
`getScenario/listScenarios/getSceneConfig/listMappings/getScenarioKeyForEncounter` · `launchFromEncounterCtx/launchForEncounterKey/launchScenario` · `testFire` (GM console; `ctx.source==="manual-testFire"` suppresses ALL world effects) · `hasScenario/registerScenario/unregisterScenario/clearExternalScenarios` · `registerCampaignBeatScenario`.
`_launcher.playScenario` (`scene.launcher.js:1627`): per step activate scene → generic `scenario.spawn.actors[]` → **`spawnFactionPCs`** (2026-08-27, re-ruled 2026-08-30: **stewards only** — `actorKind()==="steward"`, crew/association echoes excluded — for ALL factions on the ACTIVE CAMPAIGN's roster `campaign.factionIds`+primary, ctx factions as campaign-less fallback; skips stewards already on scene, bottom-center row, FRIENDLY) → bespoke `spawner.run` (registry has ONE entry: `bandit_ambush_standard`, hardcoded ids). Then outcome prompt → war log → auto-return (skipped if the beat path dived). `applyWorldEffectsForOutcome` (~1300 lines of per-scenario switches) divides/multiplies opBank by OP_TO_MARKS on read/save.
`_spawner.{run, SPAWNERS, spawnAtCenter, spawnActors, spawnTokens, spawnFactionPCs}` · archetypes `getArchetype/listArchetypes` (🟡 zero consumers) · outcomes `getOutcomeSetForScenario/listOutcomeSets`.
**Settlement (2026-08-28 refactor)**: `await runBeat()` is the DECLARED chain-completion event (the campaign engine awaits every choice hop, verdict hub, and in-chain retry; ctx + `__chain` marker threaded through all hops). After it returns, `settleEncounter` waits only on real combat (6s grace for (end)-door fights to reach the tracker, audible "End Combat to move on" notice, 45-min hard stop), then returns the table + nudges/suppresses, and **emits `bbttcc:encounterChainSettled` + stamps `game.bbttcc._encounterChainSettledTs` + socket-relays `{t:"bbttccEncounterChainSettled"}` over `module.bbttcc-campaign`** (listener registered per client at ready). All window/title polling deleted. No settings (reads `bbttcc-campaign.encounterTables` cross-module), no tools, no styles. ⚠ declared circular dependency travel↔encounters.
**Travel-table entry contract (2026-09-03)**: `encounterTables` entries support `conditions.terrain`/`terrains` PLUS **`conditions.phaseGte`/`phaseLte`** (entry invisible to the roll while `bbttcc-campaign.storyPhase` is outside the band — how Act-1 curation reopens by itself at Act 2) and **`once: true`** (entry invisible after its beat appears in `directorState.firedStoryBeats`; a DECLINED encounter never fire-marks, so it can return). Both enforced in `api.travel.js` `passesTravelConditions` / `pickEncounterFromCampaignTables`. Authoring macro: `bbttcc-campaign/tools/patch-act1-travel-table.macro.js`.

---

## 12. bbttcc-raid

*Raid & Siege Engine* v1.3.14.4 — 104 esmodules, ~40k lines. Raid console, maneuver catalog, strategic activities, **Courtly Intrigue**, **Tableau**, **Siege**, **Agent API**, orbital strikes.

### 12.1 Raid core
Flow: `api.raid.openConsole({factionId})` (`module.raid-console.js:6693`; smart-routes players into in-flight raids) → session state at `flags.bbttcc-raid.raidSession` (rev-guarded; player writes relay `{t:"raidSession"}` to GM) → per-round maneuver gating → `resolveRoundWithManeuvers` (`raid-roundflags.enhancer.js:34`) or scenario engines → `applyPostRoundEffects` (wrapped by victory/morale enhancers) → world mutations via `api.worldMutation.applyWorldEffects` → war log.
**Maneuver catalog authority** = `compat-bridge.js` `EFFECTS` (`:322`; published `api.raid.{EFFECTS, listEffects}` `:751`), hydrated by JSON loaders (`data/bbttcc_maneuvers_v1_4.json` etc.) + sprint/courtly/balance content enhancers + Character-Option injection (⭐ [Option] entries). Raid types (`raid-maneuvers.js:24`): canonical `violence ⚔ / intrigue 🥷(→Alarm) / presence ♕(→Courtly)`; 11 legacy keys deprecated.
**`_canFactionUseManeuver` gate order** (`module.raid-console.js:7618`): 0 tier SCALE (maneuver tier→faction tier 1/2/3; crew grants arm access, never scale) → 0.1 crew/occult grant short-circuit → 1 doctrine ownership (fail-open at zero doctrine items) → 2 learned/unlock (`flags.bbttcc-factions.unlocks.maneuvers`) → 3 option-derived roster check (early return) → 4 rig-role (rig_combat only). GM bypasses unless `ignoreGM`.
Other API: `canUseManeuver`, `crewGrants{crewMap,occultMap,classMap}`, `TYPES/getTypes/resolveCanonical/getManeuvers`, `STRATEGIC_THROUGHPUT/auditStrategicThroughput` (`strategic-throughput.js:652`), `resolveRaidRound`, `anytimeBudget`, `sceneIntents`, `forcePlanningMode`, `orbitalStrike/orbitalStrikeHex/findOrbitalBunker` (`orbital-strike.js:357`), `openBossBuilder`, `pickTargetHex`.
Hooks: emits `bbttcc:raid:maneuver:fired` (`:1931`), `bbttcc:raid:roundCommit` (`:6591`), `bbttcc:raid:canvasVfx`. Socket branches on `module.bbttcc-raid`: raidSession/raidCinematic/raidPill/raidVfx/infilHook/courtlyHook/siegeRequest(+Result)/siegeSceneSwap/siegeHook/courtlyPlaySecret. Setting: only `bossState`. Activity Planner = `module.raid-planner.js` (strategic-only).

### 12.2 COURTLY subsystem (spec `COURTLY_INTRIGUE_SPEC.md`)
**Influence engine** (`raid-courtly.influence.enhancer.js`) — social combat as HP duel. `api.raid.courtly({attackerId, defenderId, atkInitDip, atkInitSoft, ...})` (`:322`); Influence HP = `10 + dipOP + softOP/2`; four actions/exchange **persuade/inspire/expose(Scandal on hit-by-6+)/intimidate(violence presence bonus, backlash on fail-by-5+)**; round cap 6; Suspicion track 0..10 (≥10 = court collapse). Scenario object (18 methods incl. `step, applyEffects, drawSecret, converseSecret, spendFavorAndBoost, armLastWord, burnScandalScar`) mirrored at `api.raid._lastCourtly` — ⚠ **GM-client only**; non-GM paths need socket relays (only `playSecret` has one). Read-only state mirror on both factions at `flags.bbttcc-raid.courtlyActive`.
Scenario-effect vocabulary (`applyEffects` `:1162`): `suspicionRise/Fall, queueRollMod, queueReroll, queueActionBonus, clearScandal, favorShift, discardSecret, lockSpend, armLastWord, drawSecret, converseSecret, spendFavor, noteForGM, burnScandalScar`. Outcomes: cleanTriumph / tarnishedVictory / publicHumiliation (+3wk `disgracedUntil`) / stalemate / mutualRuin; scars at `flags.bbttcc-raid.scandalScars` (light −2 r1, heavy −3/−1), one-shot `nextCourtlyInfluenceCapDelta`. **`courtlyDoor` flag** `{defenderId, method:"invited"|"conceded", npcName}` — stamped by mal-voice court doors, consumed at scenario creation (invited +2 exch-1; conceded +1, Suspicion starts 1). Courtier favor at actor flag `courtFavor{[factionId]: -3..3}`; courtiers = tokens flagged `tableauActor`.
**Secrets API** (`raid-courtly.secrets.api.js`) — `api.raid.courtlySecrets.{addSecret, playSecret, getSecrets, enforceCap(5), EFFECT_KEYS(15), EFFECT_INFO, describeEffect, normEffectKeys}`. Secrets = feat Items from pack **`bbttcc-master-content.courtly-secrets`**, flag `flags["bbttcc-raid"].secret = {acquisition:"earned"|"stolen", effectKey|effectKeys(compound "a+b" fire in order), raidId, acquiredAt, source{npcActorId,speakerName}}`. Effect keys: `rollPlus2/3, forceReroll, influenceDmg2/3, clearScandal, favorPlus1/2, favorShift, coverTracks, doubleAgent, oppRollMinus2, stirThePot, discardStolen, freezePurse`. Play = handler chain → item deleted → stolen adds +2 Suspicion → **betrayal memory** written to the divulging NPC via `mal.npc.addMemory` + hook `bbttcc:dialogue:secretBetrayed`. Non-GM play relays `{t:"courtlyPlaySecret"}` (activeGM-guarded).
**Acquisition**: `drawSecret` (blind pack draw) · **`converseSecret`** (probes tableau courtiers via `mal.npc.armedSecretCount`; "Work them"/"Listen in" opens a REAL mal-voice conversation — `readTheRoom` = NPC approaches, `eavesdrop` = unobserved, slipped truth = stolen; secret arrives asynchronously through the divulge/approval flow) · outcome awards · maneuvers Read the Room / Eavesdrop / Forged Letter.
**HUD** (`raid-courtly.hud.js`) — three draggable panels (Influence+Suspicion w/ secret DROP ZONE · Courtier Roster w/ GM favor pills · Secrets & Leverage, viewer-scoped, drag-or-click Play), gated on open console + `scene.flags["bbttcc-raid"].tableau.enabled`. **VFX** consumes `bbttcc:courtly:vfx`. Faction-sheet badge strip (Scandal/Disgraced/Inf-delta). **12 courtly anytime maneuvers** (`maneuvers-courtly-content.enhancer.js`: whispered_aside, public_toast, plant_a_doubt, the_last_word (Cosmic Linguist), quote_old_law, read_the_room, stage_distraction, forged_letter, sidle_closer, eavesdrop, call_question, patrons_word, mask_off).

### 12.3 Tableau (`tableau.canvas.js`, spec `TABLEAU_SUBSTRATE.md`)
Forced-perspective canvas substrate; scene opt-in `flags.bbttcc-raid.tableau.enabled`, token opt-in `tableauActor`. Visual-only (`mesh.scale/sort` — never persisted; mechanics untouched). Config: axis y|elevation, frontY/backY, minScale/maxScale, curve, `stageTop/stageBottom` (letterbox band — the REAL art extent), `tokenSize:6`, `baseElevation`. ⚠ depth **multiplies** the texture-fit base scale (`fitScale` `:96`); token Y = CENTER on the line. Auto-enrol + auto-size at `preCreateToken` (1×1 grows to tokenSize; structures exempt). GM Token-HUD 🎭 pin toggle; PIXI front/back/bounds guides.
API `api.raid.tableau.{DEFAULTS, readConfig, enable, disable, sizeExisting, setStageBounds, stageBand, setFrontBack, markActor, setDepth, applyAll, showGuides, hideGuides}` (`:468`). Tuner UI = `bbttcc-master-content/tools/tableau-tune.macro.js` (+elevation variant). Companion `tableau.directional-art.js` (`api.raid.directionalArt`; the module's only two visible settings: swap on movement/rotation).

### 12.4 Siege (15 files; specs `SIEGE_RAID_TYPE_SPEC.md`)
`api.siege.*` (`siege-state.js:661`): state CRUD (`getState/setState/clearState/list`), `makeSiegeState` (layers w/ breach tracking, supply, musters, threat vectors, renewal pool, champion locks, pendingTerms), `bfsSupplyPath` (real BFS hot-replaces Phase-A stub), `validateDepot`, `joinSiege/siegeForFaction/findSiegesUsingHex`, `shaveBuffer/bufferTotal/topUpBuffer/restoreLayers`, champions (`snapshotChampions/applyChampionStatusChange/openChampionDuelDialog`), `convene` (Breach Scene spine: view bound scene + `siegeSceneSwap` broadcast + openConsole + `bbttcc:siege:convene`), `conveneRelief`, `openTrojanHorseDialog`, `formUpBoth/recallMuster`, `clashManeuvers/fireManeuver/resolveClash`, `applyOutcomeWriteback/OUTCOME_MATRIX`, `layerBreaches/breachCurrentLayer`, `bindScene` (`flags.bbttcc-raid.siegeHexUuid`), `previewVfx`, tables `TERRAIN_MODIFIERS/SIZE_PROFILES/...`.
**Siege HUD** (`siege-hud.js`) — appears whenever ≥1 siege active (no console needed): supply/buffer/layers/garrison/champions/muster + GM action strip; non-GM join relays `{t:"siegeRequest"}`. **Siege VFX** (`siege-vfx.js`) — 28 hooks incl. 9 clash beats; Sequencer/JB2A volleys (⚠ fragile post-PIXI-v8 — `siege-bombardment-diag.macro.js` exists for this).

### 12.5 Agent API (spec `AGENT_API_SPEC.md`)
`api.agent.{registerVerb, capabilities, invoke, validateAgainstSchema, registerSocketEnvelope, socketEnvelopes, validateSocketEnvelope}` (`agent-registry.js:363`) — schema-validated verb registry (layers query/simulation/action), telemetry hook `bbttcc:agent:call` on every invoke. ~25 query/sim verbs (`agent-verb-schemas.js`: snapshot, audits, observation, legal actions, travel/raid estimates, candidates, recommendNext, simulate.maneuver, gm.* advisors) + 9 action verbs (`agent-action-verbs.js`: world.setState, faction.setMorale/bumpMorale, stockpile deposit/withdraw/transfer, manifestation.applyDamage, territory.deployHolding/recallHolding). Engine = `bbttcc-agent-api.js` (3.6k lines, reads `bbttcc-campaign` settings cross-module). ⚠ socket envelope registry declares 5 of the 10 actually-handled message types; `validateSocketEnvelope` never called by emitters.

### 12.6 Tools (16; NO DRY_RUN convention here — GM-guards + read-only design instead)
`raid-gauntlet-static-audit` (READ-ONLY sweep for grant orphans/tier sanity/etc.) · `raid-gauntlet-foundry` (forge: idempotent RAIDGAUNTLET test docs) · `raid-gauntlet-runner` (drives live surface under a Dialog autopilot — owner-run) · `setup-raid-test-scenarios` (⚠ mutating, no GM guard; header = best prose description of the maneuver gate order) · `siege-perspicacity-setup` (hardcoded ids) · `end-siege` (the End Siege button the engine never shipped — teardown w/ confirm) · `agent-api-selftest` · `siege-phase-{a,c,d,e,f}-selftest` · `siege-bombardment-diag` · `hammer-of-god` (tactical orbital strike) · `orbital-strike-hex` (strategic).

---

## 13. bbttcc-mal-voice

*The AI voice layer* — BYO-key LLM module, two halves: **voice registry + trigger engine** (Mal, The Watcher, GM Advisor, Faction Advisor, Operator) and the far more finished **NPC dialogue engine** (`npc-dialogue.js`, ~2.8k lines): any non-player-owned token becomes a live conversational NPC with persona assembled at send-time and **Anthropic tool calls as the seam back into game mechanics**.

### API — `game.bbttcc.mal.*`
`settings.{get,set,provider,apiKey,model,endpoint,budget,policy,debug}` (`module.js:132`) · `lore.{getPrimer,getDefault,setOverride,clearOverride,enabled}` (`lore-primer.js:158`) · `providers.anthropic.{call, DEFAULT_MODEL, COST_TABLE, estimateCost}` (`providers/anthropic.js:444`) · `voices.{register,unregister,get,list,enabled,setEnabled,...}` (`voice-registry.js:221`) · `triggers.{fire, defaultContextBuilder, slimSnapshot, truncateForLLM}` (`trigger-engine.js:309`) · `output.{render, beginStream,...}` · **`npc.{talkTo, editPersona, addMemory, nudge, armedSecretCount, setTestChoices, _buildPersonaPrompt, _gatherWorldLore, _gatherDossier}`** (`npc-dialogue.js:2698`).

### Backend
Browser fetch → `api.anthropic.com/v1/messages` (`anthropic-dangerous-direct-browser-access`). Default model `claude-sonnet-5`; per-model request sanitization; prompt-cache blocks (1h on the lore primer — deliberately fat 6.5KB so it clears the cacheable minimum); SSE streaming w/ tool_use reconstruction; retry on 429/5xx and empty-refusal; cost estimator; 200-entry `callLog`.

### NPC dialogue flow (`_send` `npc-dialogue.js:1618`)
Prompt = [lore primer, cached 1h] + [persona, cached] + live tail (STORY MOMENTS / WAYS YOU CAN OPEN / SECRETS YOU GUARD / COURT DOOR / live courtly board / THE ROOM IS LISTENING) + full history. One tool round per message (`toolChoice:"none"` on continuation).
**Persona** (`_buildPersonaPrompt` `:649`): WHO YOU ARE → YOUR STORY → SHARED HISTORY (durable `memories` flag, cap 30) → COMMON KNOWLEDGE (journal `npcCommonJournal`, default "NPC Common Knowledge", 9k chars) → **THE WORD GOING ROUND** (journal `npcWorldDigestJournal`, default **"The Turn Press"**, 4k chars, re-read EVERY send; relayed as neighbor gossip, no "turns/editions") → PEOPLE & PLACES (dossier) → WHAT THE CHRONICLE SAYS (keyword sweep; only FIRED beats when story state available) → YOUR PRESENT MOMENT (authoritative DONE/UNDERWAY/**NOT YET**, overrides everything) → PRIVATE TRUTH → HOW TO SPEAK (anti-confabulation rule 2b `:704`).
**Journal page tags** (`_parsePageTags` `:464`): `@after: <questId|beat:<beatId>>[:completed]` (fails CLOSED — never leaks the future) · `@knownBy: <actor|faction:X|all>` (dossier pages injected whole, tiered personal→faction→all under a 12k budget).
**Speaker identity** (`_speakerActor` `:878`): controlled token → `user.character` → user name; turns stored `"<Name>: <line>"`; recognition = `@knownBy` dossier pages + durable memories (no name-matching gate). Player/NPC boundary by OWNERSHIP (`_refusalReason` `:2353`).
**Tools the model can call**: `divulge_secret` (closed enum of ARMED secrets — GM arms them in the 🧠 persona editor, one per line `Label :: effectKey :: unlock :: truth`; genuine unlock → `_grantSecret` clones from `bbttcc-master-content.courtly-secrets`, stamps `secret.source`, calls `courtlySecrets.addSecret`, marks spent in `persona.secretsUsed`, writes NPC regret/trust memory; GM convos confirm inline, player convos post a GM approval card) · `open_court_door` (stamps `courtlyDoor` on the faction) · `court_notices` (suspicion +1 murmur / +2 stir) · `point_the_way` (mid-conversation doors incl. NPC hand-offs that open the next NPC's window) · `enact_story_choice` (campaign beat pipeline). All fail-soft when APIs absent.
UI: dialogue window; 🧠 persona editor (topics/private truth/drag-drop secret arming — Courtly Secret Forge rows drag onto it); 💬 Token-HUD button; keybind **Y** = speak with hovered NPC (players, no ownership needed); intro-audio on open; 5 chat-card families. `nudge()` = NPC speaks first.

### Settings / tools
World: `apiProvider/apiKey/model/customEndpoint/monthlyBudgetUSD/defaultPolicy/debug` + hidden `registeredVoices/callLog`; `useLorePrimer/lorePrimerOverride`; `dialogueEnactMode/dialogueIntroAudio/npcCommonJournal/npcWorldDigestJournal/npcDialoguePlayers`.
Tools: ~20 GM seeders — world dossier (base + full 41-entity), common knowledge, per-region dossiers (Allesh-Gilliam/Khezek/Lyrenn/Valhaulan/Garden), 4 hex-vignette packs (⚠ each requires its bbttcc-campaign sibling seeder FIRST), rite/seal ending pages (branched via `@after: beat:`), receipts foreshadow, prisoner-rite truths, persona patches, `mal-voice-smoketest`, **`audit-persona-coverage` (read-only, 2026-08-27)** — maps 🧠 persona coverage (topics/truth/armed-vs-spent secrets/court door) across the cast, ranked by story weight (speaker beats ×3 + actors[] refs + scene presence + faction), buckets 🔴 unarmed story NPCs / 🟠 partial / 💤 spent-out / 🟢 armed; console report + GM card. **`seed-persona-wave1` (DRY_RUN, 2026-08-27)** — arms the audit's top canon-grounded targets: Dougan Marsh (full kit incl. doubleAgent feud secret + summit court door), Captain Robot (forceReroll receipt-pattern secret + clean-call door), Road Bandit/Razor Raider light personas on ALL copies, stage-direction notes for the speechless creatures; deliberately skipped Joans/Errata courtiers/Sweet Release pending owner input — resolved by **`seed-persona-wave2-crews` (DRY_RUN, 2026-08-28)**: voice-only personas for Marginalia's six Errata crew/Association echoes, Toblerone's Sweet Release pair (first-draft flavor), and Avuncular Joans (small-talk-as-art, canonizes nothing). **`vo-polish.py` (offline dev tool, 2026-09-07; `bin/vo-polish` symlink)** — ffmpeg batch voice-over cleanup for recorded NPC lines (declip → high-pass → per-file auto-notch → RNNoise → expander → compressor → two-pass loudnorm to −16 LUFS; `--radio` band-limits/squashes/adds gated static); writes `_polished/` or `_radio/` beside the sources. Needs ffmpeg + `~/.local/share/rnnoise/*.rnnn`; numpy (via `~/.venvs/overlay`) only for the notch hunter.

---

## 14. bbttcc-market

**2026-09-04 — bestiary hire:** `resolveItemFlagPrice` charges `price.hire` for lineage-flagged Actor docs (bounty is what a faction earns, hire is what it pays); `hire === null` (boss) → priceless. Tool `tools/seed-bestiary-hire-catalog.macro.js` seeds vendor "The Muster".

Single-file procurement console (`scripts/market.js`, ~2k lines). Faction spends OP/marks on catalog entries of 7 kinds (gear/rig/rig_upgrade/facility/facility_upgrade/hex_asset/actor); **Economic Horizon** pricing (rarity-vs-horizon distance; in-horizon gear can be free "Standard Issue"; artifacts refuse purchase); GM catalog + vendor editor.
API `api.market.{purchase, openMarket, openCatalogEditor, listVendors, listCatalog}` (`:1918-1952`). Purchase validates ALL delivery prereqs before spending (⚠ OP engine has no rollback). Gear delivery via `game.fourththing.stack.orCreate` (`:723`, 2026-08-27) w/ legacy-type coercion + `origin:"vendor"` stamp; **untrained-hands warning chips** on weapon/armor rows via `equipProficiency` (`:1478-1522` — warning, not a block). Settings: `vendors/catalog/lastContext` (hidden). Registers 20+ tips into `game.bbttcc.help` under `"market"`. Faction-sheet header "Market" button. No tools.

---

## 15. bbttcc-onboarding

**Founding parity seed (2026-09-10)** — `incarnation-forge.js foundPlayerFaction` now seeds `tier:0`, flat tier-0 `opCaps` (50 marks/bucket) and morale/loyalty from the standard package's `tracks` (25) at `Actor.create`; previously only isFaction/roster/foundedBy/creed, so onboarding factions had no caps (turn-driver sized logistics off the bank) and the sheet's first save wrote morale/loyalty 0. Existing victims: `bbttcc-factions/tools/repair-onboarded-faction-parity.macro.js`.

Diegetic tutorial director — **The Operator** walks a new player through 17 beats (`beats.js:2689`: incarnation → meatsuit → driving (Test Track) → stewardship claim/turn → outfitting → crew_occult → surge → manifestations → combat_sim → travel → 3 raids → proving_trials → final_showdown → graduation) using the player's REAL steward/rig/faction. All scaffolding flagged (`flags["bbttcc-onboarding"].spawned` / `tutorialScene`) so teardown only ever deletes its own.
API `game.bbttcc.onboarding.*`: `settings/user/resolve/wireScene/sceneWiring/setKickoffMap` (`module.js:296`) · `beats.{register,list,get}, start, skip, reset, status, activeRuns, isRunning, ui.*` (`director.js:435`) · `speak/riff/operatorAvailable/screen.*` (`operator-voice.js:277`) · `runAsGM` + relay ops (`relay.js:96`) · **stage.js 30+ staging helpers** (spawnDummy/mintRig/grantOp/spawnFoe/claimHex/setRaidSession/beginShowdownCombat/teardownFinale/cleanup/...) — all privileged ops relay to lowest-id GM · `tours.{register,list,get,start,stop,menu}` + `offerTour`.
**Lane system** (`stage.js`): `activeRuns` world-setting registry, 0.17 lane separation, 15-min staleness — parallel-safe concurrent tutorials. **18 tours** (`tour-defs.js`) — step text reads `game.bbttcc.help` (tours + tooltips = one source). `tour-beats.js` wraps beats to offer tours + resume lines; first-open offers wrap Planner/Raid Console opens. `incarnation-forge.js`: no steward → Sorting Engine Tree Wizard v2; no faction → founding ceremony.
Emits `bbttcc:tour:started/ended`. Settings: `completed/offerOnReady(log-only)/activeRuns/campaignWakeBeatId/campaignResumeBeatId`. Tools: `onboarding-setup.macro.js` (authors tutorial scenes — owner-run), `wire-tutorial-scenes.macro.js` (GUI re-pointer), `tour-menu`, `repair-op-overcap` (report-first).

---

## 16. bbttcc-master-content

The content pack: 11 declared compendia (`ancestries classes subclasses items vehicles npcs npc-abilities doctrines courtly-secrets documentation scenes`) + `packs/_source/` JSON canon (2,034 files) + **tools/ = 202 in-Foundry macros + 12 node scripts** (the authoring/repair toolchain). Runtime script = only `np-ancestries-router.js` (4 non-playable ancestry families → CHAR_OPT_ABILITIES dispatch).
**Pack canon status**: `_source` exported for 10/11 — 🔴 **`subclasses` has NO `_source` export** (LevelDB only). 🔴 **`heritages` LevelDB pack exists on disk but is NOT declared in module.json** (never loads); same for loose-JSON `ancestry_feats`. ~53MB of backup archaeology in the module root (3 archives, stale item packs, a shell-glob-accident directory).
**Keystone (classless build) — 2026-09-05**: `bin/ft-keystone-audit` (node, repo root; no Foundry needed) reads `packs/_source/classes` and writes `KEYSTONE_AUDIT_<date>.md` + `tools/keystone-worksheet.json` — per-feat path/doctrine, unlock level (stamped vs prose vs NONE), payload, router wiring, proposed kind (keystone/principle/refinement). Spec `~/KEYSTONE_SPEC.md` v0.1 DRAFT (8 open rulings); planned flag `flags.fourththing.keystone {kind, engine, chain, gate, cost}` + `system.prerequisites.items` (currently populated on 0 feats). No runtime consumer yet.
**Tool families** (see survey 2026-08-27 for the full 202-entry catalog): AA animation stampers [DRY_RUN,GM] · **Courtly**: `courtly-secret-forge.macro.js` (interactive secret-template builder — rows drag onto mal-voice personas), `create-courtly-secrets` (3-sample seeder), `seed-courtly-maneuvers` [D] · maneuver catalog/balance sync ×14 · **`tableau-tune.macro.js`** + elevation variant (live tableau tuning panels) · leygates/harvest-node setup · gear/item creators ×20 · rigs/bosses/bestiary seeders ×17 · pricing stamps · class/path/doctrine authoring ×30 (incl. 9 prose-body rewrites) · **`create-starter-kit-manifestations.macro.js`** (seeds the 81 path/doctrine starter-kit manifestations into `items` under "Starter Kits — Path Manifestations", stamped `flags.fourththing.starterKit {path, doctrine, core}`; self-linting, idempotent; consumed by `game.fourththing._progression.grantStarterManifestations`) · effects/triggers/passives stampers · D&D-vocab scrubs (phase 2–7) · ancestry seed-or-scrub consoles · pack plumbing (`compile-loose-packs`, `probe-pack-duplicates`) · targeted playtest repairs · **Steward Gauntlet** 3-phase harness · read-only audits (`audit-aa-coverage`, `price-audit`, `audit-maneuvers-fire-mode`, ...).

---

## 17. bbttcc-sorting-engine

Character creation by **Tree-of-Life quiz**: ten sefirot stations → ~20 answers → 29-trait vector → full build resolution. Two generations both load; **v2 is canonical**.
API: `game.bbttcc.sortingEngineV2` (`bbttcc-sorting-engine-v2.js:436`: `scoreQuiz, resolveCategory/Alignment/Ancestry/Occult/Philosophy/Archetype/Crew/Class/Doctrine, runFullDescent, computePillarTally(mercy/severity/neutral pillar readout), assembleWizardInputs, writePillarTally(seeds AAE drift)`) · **`game.bbttcc.openTreeWizardV2(actorOrNull)`** (`wizard-v2:2535`; Descent → Review ("Your descent suggests…", overridable) → Finalize; applies via auto-link's `runGuidedCreatePipeline`) · `game.bbttcc.dumpSEPacks` diagnostic. Spec `data/bbttcc_sorting_engine_v2_full_spec.json` (resolver order alignment→ancestry→occult→philosophy→archetype→crew→class→doctrine; heritage + faculties deliberately player-picked, Option B 2026-04-23). Consumers: onboarding incarnation-forge, auto-link Create button. 🟡 whole v1 generation (`api.sorting.*`) loads with zero consumers.

---

## 18. bbttcc-aae

*Adaptive Adversary Engine* — faction moral/political model. **Live layer**: 8 hardcoded political philosophies (`POLITICAL_PHILOSOPHIES` `module.js:31`) stored at `flags.bbttcc-aae.politicalPhilosophy`; **drift pipeline** `applyPoliticalImpact({factionId, actorIds, tags, source})` (`:448`) scores policy tags vs roster philosophy distribution → `driftScore` −100..100 (called by bbttcc-campaign `:3589` — "🗳️ Political Pressure" cards). API `game.bbttcc.api.aae.{listPoliticalPhilosophies, get/setPoliticalPhilosophy, getPoliticalTagCatalog, readFactionDriftState, writeFactionDriftState, applyPoliticalImpact}` + 🟡 dead Moral Profile POC (`generateMoralProfile/getMoralProfile/suggestPreferredOps` — zero callers). No hooks, no UI, no settings, no tools. ⚠ Philosophy flag read directly (not via API) by bridge/raid/factions/auto-link; sorting-engine wizard ships stub descriptions with a "wire pending" TODO.

---

## 19. bbttcc-auto-link

*Character Builder* v0.8.2 (name is legacy) — actor-creation surface + character-sheet layer. Four "Create …" directory buttons (RFI Character→Tree Wizard v2, NPC, Monster, Rig; native Create hidden); three authoring dialogs; two marker sheet subclasses ("Bad Eden Character/NPC Sheet"); DOM-injected **Bad Eden Identity tab** (identity dropdowns, enlightenment, Tikkun sparks read-only, ▶ Use feature buttons, aptitude-grant repair) + multi-sheet tab healer.
API: `api.autoLink.{runGuidedCreatePipeline(vestigial), getGuidedState, inspectIdentity}` · `api.npcBuilder.{open, mintEchoMember, mintEchoRoster}` (`npc-builder.js:1066`) · **`api.monsterBuilder.{open, lineages, qliphoth, banners, envelopes, loadExemplarIndex, listExemplars(lineage), saveAsExemplar(actor), categories(legacy), templates(legacy → exemplar cache)}`** (`monster-builder.js`, lineage edition 2026-09-04: eight lineages with signatures, exemplars read from the NPC Pack + world via `rfi.actor.lineage`/`exemplar` flags, boss bracket + Fractured Will/Signature/Ultimate kit, chip pickers over `FT.DAMAGE_TYPES`/`FT.CONDITIONS`, stamps `lineage/subLineage/title/qliphah/creatureType/price`) · **`api.bestiary.{METHODS, isBestiary, bountyFor, listFactions, offer(actor), resolve(actor, {method, factionId, marks?, messageId?})}`** (`bestiary-bounty.js`, imported by monster-builder.js): bounty credited to the pool matching the METHOD (kill→violence · talkdown→nonlethal · parley→diplomacy · capture→intrigue · cleanse→softpower · rite→faith), clamped to the bucket cap via `op.preview`, stamps `rfi.actor.resolved` (pays once); auto-offers a GM-whispered card on the 0-Integrity crossing (primary GM). **Emits `bbttcc:bestiary:resolved {actorUuid, factionId, method, pool, marks, credited, lost, by}`** · `api.rigBuilder.{open, seedFromActor, mintFromChassis, computeRigCost}` (`rig-builder.js:1844`). Emits `bbttcc:enlightenmentChanged` (🟡 zero listeners). Global aptitude guard normalizes `downgrade→upgrade` on skill grants (`character-wizard.js:55-80` — the live part of that file). Creation wizard auto-grants the path/doctrine starter manifestation kit post-import via `game.fourththing._progression.grantStarterManifestations` (3 items non-TCC / 8 TCC; soft no-op on pre-kit system builds). Tools: `backfill-monster-creaturetype` [DRY_RUN=true] (builder-made actors only — keys on `templateCategory`); **`bbttcc-master-content/tools/backfill-bestiary-lineage.macro.js`** [DRY_RUN=true] — stamps `rfi.actor.{lineage, subLineage, tier, bracket, price}` + `creatureType` and normalizes damage/condition/sephirah vocabulary across the NPC Pack's *Bad Eden Monsters* folder (lineage roster inside; `elite`→`boss`). Builder (2026-09-04): writes system damage canon (retired `energy` mapped by flavor), offers `qliphoth` sephirah, prices via `computeCreaturePricing`, `details.level` = 1/8/13/18. Lineage-tab rebuild SHIPPED 2026-09-04 (audit §4); builder runs on **DialogV2** (`foundry.applications.api.DialogV2.wait`, form-wrapped, chips are type=button). Pass 3 tools: **`bbttcc-master-content/tools/scrub-5e-ports.macro.js`** [DRY_RUN=true] — rewrites the 14 5e-ported creatures into RFI vocabulary (Fractured Will / reactions / reroll-highest / Restrained-escape / Staggered), strips flat +N damage, sets ranges in squares, writes Concept+Signature bios, bodies for the seven empty `power` items with correct damage types, renames Scavenger Beast / Wastes Stalker / Qliphothic Shambler; tags `rfi-scrubbed`. **`bbttcc-market/tools/seed-bestiary-hire-catalog.macro.js`** [DRY_RUN=true] — vendor "The Muster" + `kind:"actor"` entries for hireable lineages (mortal / wild / pre-fall, non-boss).

---

## 20. bbttcc-character-options

v1.2.0 — owns character **identity** (archetype/crew/occult/political/alignment/enlightenment), the **Tier Engine** (tiered feat sync to level), the OP scanner (9 buckets off items+AEs), and the **enlightenment ladder** (one reconciled AE, Clarity aura to nearby friendlies, once-per-Soma-Break **Minor Miracle**). 7 compendium packs.
API: `api.characterOptions.{recalcActor, recalcAll, refined{data,getOwnedOptionCounts,getUnlocksForActor}}` (`module.js:41-68`) · `api.identity.{getIdentityFlags, setIdentityFlags, syncOptionTiers, getTierForLevel}` · `api.enlightenment.{miracle, canMiracle, miracleAvailable}` (`enlightenment.js:423`). Emits `bbttcc:opsRecalculated`. Sheet row `#bbttcc-enlightenment-row` + ✦ Minor Miracle button (routes through `castManifestation({miracle:true, freeClarity:true})`). No settings.
Tools (8): enlightenment gauntlet ×3 (static-audit read-only / forge / runner) · `animate-occult` [D=true] · `convert-npcs-to-callings` [D=true] · `repair-aptitude-grants` · `fix-aptitude-grants-everywhere` (also fixes source packs) · `repair-rank-inflation` [D=true].
🟡 Perk backlog: enlightenment AE flags `opRegenBonus/healingHalved/darknessSpikes` written but implemented nowhere (only `auraClarity` + `minorMiracles` live). `refined-options` data = "no mechanics yet".

---

## 21. bbttcc-epic

*Epic Play* v0.1.0 — the **level 18–30 endgame**, fourththing-only, namespaced **`game.fourththing.epic`** (deliberate). P1 **The Gate**: Convergence when level ≥18 AND enlightenment apex (`enlightened`→keter / `qliphothic`→thaumiel), latched at `flags.bbttcc-epic.converged`, one-time beat, hook. P2 **Presence**: per-steward heat/notoriety (`flags.bbttcc-epic.presence{value,masked}`), faction aggregation, masking, bands. P3 **Repair**: World Health from protagonist-aligned hexes, Malkuth seam at 100%, `alignHex`/`integrateSpark` actions paying Presence.
API: `epic.{isConverged, convergencePath, computeConvergence, epicBand, evaluate, resetConvergence}` (`epic-gate.js:223`) · `epic.presence.{get, isMasked, adjust, set, mask, unmask, partyTotal, hunted, band, sync}` (`presence.js:247`) · `epic.worldHealth`, `epic.repair.{alignHex, integrateSpark, reach, worldHealth}` (`repair.js:266`).
Emits `bbttcc:epic:converged / :presence:changed / :malkuthAligned` (**`malkuthAligned` consumed by reformation.js since G3** — its first listener ever; the other two remain telemetry seams); listens `bbttcc:territory:hexUpdated`, `bbttcc:advanceTurn:end`, `bbttcc:spark:deposited/repaired`, `bbttcc:spark:hexSeated/hexIntegrated` (tikkun-hex: world-health refresh + Presence credit). UI: Ascension sheet panel, faction Presence chip, World-Health chip on Campaign Overview. Hidden settings `protagonistFactions/malkuthAligned`. Specs `~/EPIC_P*_SPEC.md`. 🟡 `alignHex/integrateSpark` have no UI callers; `ascent` band computed but unused (release 2).
**P3.1 (2026-09-01)**: `boardScenes()` — worldHealth counts only scenes flagged `bbttcc-epic.boardScene` (the 5 canonical region scenes = 141 hexes; stamp via `tools/mark-canonical-board.macro.js`; falls back to all scenes if none flagged). `isHexAligned` trump: a hex whose `flags.bbttcc-territory.spark.state === "integrated"` counts aligned regardless of banner or darkness (owner ruling).
**A1 — THE GAZE (2026-09-02, `adversary.js` + repair.js; `DESCENT_ENGINE_SPEC.md` Part II, 8 rulings locked)**: Reach ENFORCED — `reachBudget(fid)`/`tryDebitReach(fid)` (exported + on `epic.repair`), budget = `REACH_BY_BAND[band]` Acts of Repair per faction per Apply turn (`flags.bbttcc-epic.reachSpent`, reset by GM-gated advanceTurn listener; tikkun-hex `integrate` shares the budget). Presence economics: masked repairs earn half (round up), Shadowed+ darkness dims −1 (`effectiveRepairPresence`). **Boiling Point**: world setting `bbttcc-epic.boilingPoint` = bandPoints + 3×temples + wh%/10 + 5×Lamps, recomputed each Apply turn by the active-GM writer; 1d100 ≤ BP fires escalation (A1 = omen cards at all tiers; hunter/reprisal/corruption/fracture events = A2). API `epic.adversary.{boilingPoint, compute, census, force(tierKey)}`. Emits `bbttcc:adversary:boiled/:event/:hunter`. Tool: `descent-selftest.macro.js` (read-only).
**G1 — THE THRESHOLD (2026-09-03, `daath.js`)**: Ten Lamps detection (derived — each sephirah ≥1 integrated board spark; debounced watch on `bbttcc:spark:hexIntegrated` + Apply turns, active-GM writer), per-lamp "✦ A LAMP IS LIT" cards + `bbttcc:epic:lampLit`; at 10/10 → **Daath opens** (world setting `bbttcc-epic.daath {opened, lampsLit, risen, darkDoor, outcome}`, the `egoDragonWarning` i18n prophecy fires as a world card, `bbttcc:epic:daathOpened`). API `epic.daath.{status, knock, bindScene, check}` — `knock(actor?)` rises the Dragon (`bbttcc:dragon:risen`; activates the bound `daathSceneId` scene) through the open gate, or EARLY via the **dark door** for a Threshold-band (Darkness 10, `mayKnock`) steward; worldHealth% scales difficulty, never gates. Once open, Gaze omens draw from a dragon-weighted pool. Great Work threshold reconciled: activities data now says **three** purified Sparks (matches code + Campaign Overview). Ritual engine = G2.
**G3 — THE REFORMATION (2026-09-04, `reformation.js`)**: the ending's listener — fires when BOTH halves land in either order (`daath.outcome ∈ {parley,sunder}` × `malkuthAligned`; listens `bbttcc:ritual:answered` + **`bbttcc:epic:malkuthAligned`** (first consumer ever) and reads **`greatWorkComplete`** (first reader ever)); one half alone posts a single "the Work awaits" card. Sequence: ✦ THE REFORMATION ✦ (radiant/scarred per outcome) → per-faction 📖 epilogue cards (war-log count, VP/Unity, each steward's faced fragments + final Darkness band) → ⚖ mercy ledger card → 🌅 **Act II ascent unlock** (setting `bbttcc-epic.ascentUnlocked` + `bbttcc:epic:ascentUnlocked`; `epicBand` already answers "ascent" at L21+, release 2 builds what lives there) → 🎙 Mal's last word. Latched at `bbttcc-epic.reformation {done,at,outcome}`; API `epic.reformation.{status, fire, reset}` (reset = rehearsal mode). Emits `bbttcc:epic:reformation {outcome}`.
**G2 — THE FINAL RITUAL (2026-09-03, `ritual.js`)**: station engine (Chain → Veil → Crossing → Answer) on world setting `bbttcc-epic.ritual`; GM verbs `epic.ritual.{begin, status, next, instrument, answer, abort}`. Chain reads each roster steward's heaviest UN-FACED fragment verbatim as the attack (open fragments lock that steward's channel; tiferet unlocks); Veil rating = wh/20 + mercy bonus + lamps/2 (mercy = `bbttcc-campaign.banditMercy`, threshold 3 owner-tunable); Crossing = one instrument per lit Lamp (`INSTRUMENTS` table, protect/reveal/attack — attack spends count against PARLEY, dead lamp excluded). **Answers**: PARLEY (all present fragments faced + mercy ≥3 + ≥7 lamps unspent-or-gentle → `greatWorkComplete {mode:parley}` on roster factions, global Darkness −3 everywhere) · SUNDER (taint +2 roster, random lit lamp → `daath.deadLamp` — its temples stop yielding forever (tikkun-hex skip), `greatWorkComplete {mode:sunder, reduced}`, Aegis loot note) · ROUT (Darkness +3 roster, BP→100, Dragon withdraws — `daath.routPending` logs un-faced fragments; `knock()` refuses until all are faced, then clears = the pilgrimage, Ruling 8). Emits `bbttcc:ritual:{begun,station,instrument,answered}` + `bbttcc:dragon:{parley,sundered,routed}` — **G3's Reformation consumes `answered` + `greatWorkComplete` + `malkuthAligned`**.
**A2 — the Gaze's hands (2026-09-03)**: full event ladder live — Stirring omen · Watching **HUNTER** (marks `presence.hunted()`, world setting `hunterPending`, springs on the marked steward's next `bbttcc:afterTravel` leg, qliphoth from their heaviest un-faced fragment) · Reaching **CORRUPTION** (random dormant seated spark → `api.tikkun.hex.corrupt`; temples immune) or **REPRISAL** (aligned hex per-hex darkness +1 + GM defense directive) · Boiling **FRACTURE** (Purified hex loses Purified, darkness +2, worldHealth drops → Malkuth re-arm cause; Nadir+ stewards get the Dragon-dream whisper). Events fall back down the ladder when targetless. GM test lever: `epic.adversary.force("reaching")`.

---

## 22. bbttcc-fx-integration

*Combat FX* v0.1.0 — keyed FX registry (banners/overlays/shake/canvas pulse/JB2A bursts with fallback chains) + engine + API, plus raid-console patch, `territory.advanceTurn` wrap, Tier-A class VFX (listens `fourththing:itemAnimated`), JB2A DB self-heal (re-fires `sequencer.ready`), AA crash shim.
API `game.bbttcc.api.fx` (alias `game.bbttcc.fx.api`; surface `api/fx-api.js:505-528`): `playKey, playRolls, playScenarioShift, playTurnPresentation, playHexActivity, playSequencerEffect, register, get, effectForFamily, burstForFamily, keyExists, resolveKey, normalizeRaidType, raidToneForType, durationForKey, ...`. Consumers: raid (console, courtly, infiltration, throughput), territory turn-driver, travel scene-transition.
🔴 **Settings namespace bug**: 4 client settings registered under `"bbttcc-fx"` (`core/fx-settings.js:2`) but module id is `bbttcc-fx-integration` → reads throw, swallowed, module runs on hardcoded defaults; all user toggles inert (Appendix A #7). Tools (3): `probe-jb2a-keys` / `selftest-fx-jb2a` / `verify-doctrine-vfx` (read-only; FIRE_SAMPLES opt-in).

---

## 23. bbttcc-radiation

v13.1.16 — RP as a REAL debuff. P1 environmental (travel legs, scene fallout **zones** with drift + tinted overlay, per-turn exposure); P2 (`radiation-effects.js`) the 5-rung sickness ladder Clean/Irradiated25/Sickened50/Poisoned75/Terminal100 (`rollPenalty/integrityCap/loseReaction/loseAction/tick`), mirrored as real token-HUD conditions; band crossings roll **double-edged mutations** (boon+bane AE pairs).
API `api.radiation.{get, set, add, levelFor}` (`api.radiation.js:87`) · `tierFor/tiers/sync` (`radiation-effects.js:129`) · `zone.{getScene,setScene,clearScene}` (`zone.enhancer.js:127`; scene flag `{intensity,drift,color,radius}`) · `mutations.{list,add,remove,clear}` (`mutations.enhancer.js:195`). Storage: `system.radiation.rp` (fourththing) w/ one-time flag→system max-merge migration (`rpStoreUnified` latch). Emits **`bbttcc.mutationRoll`** (⚠ dot namespace). Listens `bbttcc:afterTravel`, `bbttcc:advanceTurn:end` (turn exposure: PCs half RP floored, −1 in safe areas). Sheet tab `bbttcc-radiation`; zone overlay; GM mutation cards. Settings: `enableAutomaticTracking` (🟡 read nowhere), `enableZoneDrift`, `showZoneOverlay`, hidden `rpStoreUnified`. No tools.
🔴 Duplicate ladders: fourththing `_ftRadiationBite` (`module.js:10040`) duplicates the tier table; comment claims it reads `tierFor()` — it doesn't (Appendix A #8).

---

## 24. bbttcc-tikkun

*The Great Work* v13.1.16 — spiritual victory condition. 30 Sparks of Light (10 sephirot × 3 kinds, pack `bbttcc-tikkun.sparks`) through identify → acquire → integrate → deposit; **method alignment** (misaligned gather corrupts; corrupted sparks block readiness and need **repair**: materials + OP + 2d10 ritual, DC drops per partial attempt); culminates in a 3-round **Final Ritual** (Invocation→Contact→Integration → faction VP/unity/darkness).
API `api.tikkun.*` (`api.tikkun.js:541`): `hasSpark, gatherSpark(+ByItem), resolveSparkItem, checkMethodAlignment, SEPHIRAH_METHOD_ALIGNMENT, getAllSparks, createConstellationForActor, markSparkPhase, identifySpark/acquireSpark/integrateSparkCharacter, depositSpark, getGreatWorkState(+ForAllFactions)` · faction `listSparks/integrateSpark/revokeSpark` (sparks.enhancer) · `beginRitual` + `_lastRitual` (ritual.enhancer `:206`) · `openRitualConsole` (`tikkun-ritual-console.js:243`) · `openRepairLedger/openRepairRitual/openDepositLedger` (`tikkun-repair.enhancer.js:553`).
Emits `bbttcc:spark:{corrupted, deposited, repaired}` + **`bbttcc:spark:{hexSeated, hexIntegrated}`** (tikkun-hex). Listens **`bbttcc:beat:resolved`** (routes `beat.sparkLink` → spark API; payload schema at `tikkun-beat-listener.enhancer.js:11-19`) + `bbttcc:advanceTurn:end` (hex-spark yield, gates apply+isGM). UI: character-sheet tab, GM faction-sheet "⚒ Spark Repair Ledger" / "→ Pending Deposits" buttons, Ritual Console AppV2, corruption flavor cards. No settings.
**Sparks-in-the-world (2026-09-01→03, `tikkun-hex.enhancer.js`)**: hex Drawing anchor `flags.bbttcc-territory.spark {key, state: dormant|integrated|corrupted, at}`; API `game.bbttcc.api.tikkun.hex.{seat, unseat, integrate, corrupt, repair, at, all, canonicalKey, parseKey, SEPHIRAH_CHANNEL, SEPHIRAH_ALIASES}` (GM verbs; **spelling fork closed 2026-09-13** — `geburah`→`gevurah`, `tifaret`→`tiferet`, `kether`→`keter` etc. accepted everywhere and stored canonical; campaign beats may write `worldEffects.hexSpark` to drive these verbs; corrupt refuses integrated temples, integrate refuses corrupted until repaired; emits `bbttcc:spark:hexCorrupted/hexRepaired`); integrated spark on a faction-owned hex pays +1 OP/turn into its sephirah's channel (marks via `api.op.commit`, one commit per faction, GM-whispered report; sephirah→channel map owner-tunable in-file). Alignment trump lives in bbttcc-epic `isHexAligned`. Seeding: `tools/seed-hex-sparks.macro.js` [DRY_RUN=true, ⚠ placement ruling pending — `SPARK_SEEDING_2026_09_01.md`].
Tools (5): `load-sparks-pack` ⚠ **DRY_RUN=false (armed)** · `seed-hex-sparks` [DRY_RUN=true] · tikkun gauntlet ×3 (static-audit read-only / forge / runner). ⚠ Structural smell: three files race to populate one namespace (api/hotfix/enhancer, all install-if-absent).

---

## 25. surge-powers

**⚠ NOT Bad Eden.** Standalone dnd5e module v1.2.0 for the **Kaeron War / Angaria** world (`the-third-kaeron-war` on the `foundry` instance). Global `game.surgePowers` (`get/max/grant/spend/set/openMenu/menu/applyEntry/profOf/editPool`). Pool at `flags.surge-powers.value`, max 2×prof (min 4). Fill: kept d20 ≥ threshold(15) +1, nat-20 explodes, **cast +2** (`dnd5e.postUseActivity`), **combat start +1** (GM-gated `combatStart` + `createCombatant` late-join guard), long rest +prof. Spend: 23 universal powers, minProf gates. ⚡ Token-HUD + sheet-header button (left-click menu / right-click editor). Settings ×6 world-visible. No BBTTCC references in either direction.

---

## Appendix A — Dormant / Broken registry

*The "what we're both forgetting" list. 🔴 = broken wire (should fix) · 🟡 = dormant (decide: wire, keep as future seam, or delete). Retire lines when resolved.*

### 🔴 Broken wires — OPEN
4. **DESIGNED (2026-09-01): `~/DESCENT_ENGINE_SPEC.md`** — the Negredo arc (personal Darkness engine + Epic P4 Adversary/Boiling Point + Ego-Dragon Final Ritual + Reformation Outcome) specced as one arc with build order D1→G3; ⚖ 8 owner rulings pending before D1. `system.darkness{value,taint,fragments}` remains reader-less until D1 ships; `darknessSpikes` closes in D3; Radiation stays the reference ladder pattern.


### ✅ Broken wires — FIXED 2026-09-10 (deployed both instances + restarted)
30. ~~Loyalty stability penalties were dead letters~~ — **wired.** `bonuses.nextTurn.opGainPct` now multiplies every channel's income in `advanceOPRegen` (clamped ±100%, note in the regen war-log line); `bonuses.nextTurn.defenseDC` is read by `module.raid-console.js _rcDefenderNextBonus(dflags)` (= authored `nextRaid.defenseBonus` + loyalty DC, clamped ±5) at all four DC sites (projection, per-round view, Add Round, contested recompute); `tickFactionBonuses` expires both after regen so each turn's loyalty phase writes a fresh value, never a running sum.
31. ~~Integration morale/loyalty bonus never landed~~ — **wired.** `territory-garrison-upkeep.enhancer.js` now APPLIES faction morale/loyalty deltas directly (run-local `dMorale/dLoyalty`: +1 short / +2 full-integration hex with paid upkeep, −1 morale per unpaid/neglected hex; clamped 0..100; "Morale a → b" log line + `moraleDelta/loyaltyDelta` on the war-log entry) and `unsetFlag`s the stale `turn.pending.{moraleDelta,loyaltyDelta}` slot. Hex-level `turn.pending.loyaltyDelta` (consumed by the hex pending sweep) unchanged.

### ✅ Broken wires — FIXED 2026-08-28 (deployed both instances + restarted)
1. ~~actor-kind.js not in esmodules~~ — **false alarm**: ES-imported by `module.js:173` + `migrations/index.js:32`; manifest-invisible but fully live.
2. ~~`resetLegendaryOnSceneChange` never fires~~ — added to the ft-progression import and the `_progression` export object.
3. ~~`species` missing from system.json itemTypes~~ — added (constructor validates against this list, per `module.js:22700` comment).
6. ~~socket envelopes 5/10 declared~~ — all 11 message types now declared in `agent-socket-envelopes.js`; **raidPill schema corrected** (handler reads `key/label/raidType`, never `text`).
7. ~~fx settings namespace~~ — `MOD` fixed to `bbttcc-fx-integration` in `fx-settings.js` + `fx-engine.js`; the four client FX toggles now function.
8. ~~radiation ladder duplication~~ — `FT.RADIATION_BITE` declared the single source of truth (stale comment fixed); module TIERS now syncs its mechanical fields FROM the system table at ready (labels + Clean rung stay local).
9. ~~contested-takeover mislabel~~ — removed from raid module.json esmodules (was a Fortify-Hex re-wrap risking a double defense-queue; file kept on disk, unloaded).
11. ~~campaign ledger phase-door pre-GM-gate~~ — GM gate moved above the Phase Charter calendar-door block in `_onAdvanceTurnEndLedger`.
12. ~~`bbttcc:enlightenmentChanged` orphan~~ — reclassified: it's telemetry, not broken; tracked as dormant (#17 family).
10. ~~two parallel beat injectors~~ — **RULED: travel's is canonical.** autoDebt tag synthesis, the `inject.requires` Story Director gate (via `api.campaign.gates.requiresMet`), foreshadow +15 scoring, and the GM debt prompt (per campaign:hex:beat decline gate, falls through to next candidate; non-GM clients skip debt beats) are now IN travel's `maybeInject` (`hex-travel.js`, helpers near `_beatTags`). Campaign's `injector.fire` stays as a **manual GM console tool**, annotated deprecated; its three trigger-hook listeners remain emitter-less by design.
13. ~~faction tier-advancement dormant~~ — **RULED: wired on.** `faction-tier-stability` / `-advancement-api` / `-advance-button` added to bbttcc-factions module.json `scripts` (after victorygate). Stability accrues on Apply turns (GM-gated, reads the pressure enhancer's flags); `api.factions.{getTierAdvancementReport, canAdvanceTier}` live; GM Advance Tier button raises opCaps to the marks band.
14. ~~two world-state spines~~ — **RULED: bbttcc-world canonical.** `api.gm.setWorld` now mirrors `turn.number`/`world.darkness` writes into `api.world.applyGMEdit` after its own store+audit, so the spines can't disagree; core's store remains for audit continuity + legacy readers. (One-way: api.world writes do NOT mirror back — read world state from `api.world.getState()`.)
15. ~~master-content pack gaps~~ — **double false alarm.** The 27 subclass docs live in the `classes` pack (which IS `_source`-exported); the `subclasses` pack is EMPTY in repo AND on both live instances (verified by fvtt extract 2026-08-28) — a vestige, nothing to preserve. Heritages likewise live in the `ancestries` pack (56 `_source` files); `packs/heritages/` is superseded loose-JSON source material, not a functioning pack — all code reads `bbttcc-master-content.ancestries`. Do NOT declare either. (The ~53MB backup archaeology in the module root remains a tidy-up candidate.)
16. ~~structures harden/repair "stubbed"~~ — the header comment was stale: all three dialogs (Harden/Repair/Reclamation) were fully implemented — but DEAD, because the faction picker filtered `type === "character"` while factions are `type:"npc"` + flag. Filter fixed to `isFaction` flag (both dialogs); header corrected.

### ✅ Dormant items resolved 2026-08-28 (deployed both + restarted)
17. **Advisor voices wired** (new `bbttcc-mal-voice/scripts/advisor-triggers.js`): manual consults always on — `game.bbttcc.mal.advisors.{gm({mode}), faction(id)}`, 🕯 Advisor button on faction sheets (GM + owners), `tools/consult-gm-advisor.macro.js` hotbar macro; ambient fires (scene-enter → GM Advisor, sheet-open → Faction Advisor, 60s voice debounce) behind world setting `advisorAmbient`, **default OFF** — every fire is a real API call. `bbttcc:faction:opChanged` (too noisy to pay for) and `bbttcc:raid:initiate` (belongs to bbttcc-raid) deliberately stay unemitted.
20. **Epic Acts of Repair surfaced**: converged Stewards' Ascension panel now has ⚒ Align Hex (GM hex-picker dialog, NBSP-safe names, unaligned-first) and ✨ Integrate Spark (opens tikkun's Deposit Ledger — Presence credit flows through the deposit hook).
21. **Enlightenment perks closed out**: `opRegenBonus` (0.10) implemented — advanceOPRegen wrapper in `enlightenment.js` grants +10% of the regen that actually landed (snapshot-delta, cap-respecting via `op.commit`) to factions with an enlightened roster member; `healingHalved` was **already live** (system heal path reads the qliphothic level, `module.js:15238` — the AE flags are markers, mechanics read levels); `darknessSpikes` deferred with the Darkness arc (#4).
23/27/28/29. **Housekeeping deletes** (git rm, recoverable): campaign `scripts/module.j` + `world-mutation-engine.j` + `scripts/campaign-table-editor.js` (dead dup of `apps/`), factions `faction-meters.js`, territory `hex-config-build-units.enhancer.js`, auto-link `sidepanel.js` (also unloaded from manifest), tikkun `scripts/module.js` empty shell (also unloaded — ritual-console self-initializes `mod.api`).
24-partial. `runGuidedCreatePipeline` was a **false alarm** — sorting-engine's wizard-v2 applies through it (`wizard-v2:1684`); it's the live apply path, not dead.

### ✅ Cleanup pass 2026-08-28 (round 2)
- mal-voice: provider choices trimmed to Anthropic only (the three phantom options + inert `customEndpoint` setting removed; `mal.settings.endpoint` kept, returns "").
- Radiation: empty `RadiationTracker`/`RadiationZoneConfig` shells, `openRadiationTracker`, the never-read `enableAutomaticTracking` setting, and both orphaned templates removed (also fixed a latent `mod.api` overwrite race with `RadiationAPI`).
- Campaign `bbttcc-rolls-api.js` deleted (zero consumers) — import removed from module.js.
- aae Moral Profile POC methods removed from the public API (internal tables retained; old `moralProfile` actor flags inert).
- master-content: ~16MB of untracked backup archaeology (5 archives, 2 backup dirs, 2 manifest baks, 3 stale item packs, glob-accident dir, BBTTCC stub) tarred to `~/badeden-attic/bbttcc-master-content-attic-2026-08-28.tar.gz` and deleted from the module.
- Structures manifest: vestigial `"socket": true` removed; description updated from "Phase A" to reality.
- NEW `bbttcc-raid/tools/courtly-selftest.macro.js` — read-only courtly wiring self-test (API surface, EFFECT_KEYS↔INFO parity, pack health + per-doc effect-key validity, 12 maneuvers registered/tagged, socket envelopes, mal-voice seams, scandal-flag shapes).

### 🟡 Dormant — still actionable (feature-sized, unscheduled)
- mal-voice: `sheetPopup`/`tokenSpeechBubble` output channels stubbed; `monthlyBudgetUSD` advisory-only (no enforcement).
- char-options `refined-options`: Slice-1 data, "no mechanics yet".
- raid `queue-drawings.consumer`: legacy-gated hard stop — retire or revive with the queue system.

### 📦 By design / legacy-kept (reclassified 2026-08-28 — not defects)
- Encounters legacy Encounter→Scenario registry + `testFire` + single hardcoded SPAWNERS entry + unread archetype metadata: documented retirement path — reachable only for beat-less travel encounters, warns on use. Remove once all travel encounters are campaign-authored.
- Sorting-engine v1 generation: superseded by v2, kept for console BC (`api.sorting.*`). Delete when confident.
- Travel planner pill: deliberately killed (console is canonical); `api.travelPlanner` API kept for future use. `_maybeAutoScoutSigns` stub: superseded by campaign Scout Signs tables.
- Epic `ascent` band (L21–30): reserved for release 2. Epic hooks + `bbttcc:enlightenmentChanged` + `bbttcc:dialogue:secretDivulged`/`courtDoorOpened` + `bbttcc:chase:resolved` + `bbttcc:tour:started`: **telemetry seams** — emitted for future consumers, harmless.
- tikkun hotfix + sparks-enhancer install-if-absent guards: deliberate race-safety nets.
- Territory `main.js` hex-sheet class: superseded by the enhancer (ready wins); `encounters.js` retired shim kept so legacy callers don't crash; build-unit "asset" action: documented placeholder for the future asset system.
- Banks coalition vault + thin marks channel: Phase 2 scaffold by design.
- auto-link native sheet templates: documented dead code (all Bad Eden UI is DOM-injected) — delete candidates when convenient.
- raid `bbttcc:raid:published` ghost listener + `bbttcc:courtly:scarAccrued` doc-example: harmless declared intentions.

---

## Appendix B — Hook census highlights

**The spine hooks** (emitter → key consumers):
- `bbttcc:advanceTurn:end {apply}` (territory turn-driver) → system reminder card, campaign ×6, world-digest/Turn Press, world-mutation OP schedules, travel weather tick, radiation exposure, factions momentum/pressure/victory, epic world-health, facility turn effects. **ALL new listeners: gate on `apply` AND `isGM`.**
- `bbttcc:beat:resolved {campaign, beat, ctx, outcome}` (campaign `module.js:3748`) → tikkun sparkLink, territory gate-unlocks, travel chase seam, onboarding handoff, campaign's own 10 meter subscribers, builder refresh.
- `bbttcc:afterTravel` (travel) → encounters arbitration (GM-gated), radiation, mitigation drain, campaign ledger+director.
- `fourththing.manifestationCast` / `fourththing.overshoot` / `fourththing:itemAnimated` (system) → campaign adversary draw, mal-voice Watcher, fx class VFX, onboarding gates.
- `bbttcc:raid:roundCommit` (raid) → system, campaign director, mal-voice Mal.
- `bbttcc:courtly:state` / `:vfx` (raid, socket-relayed) → courtly HUD, mal-voice live court board.

**Orphans** (emitted, never consumed): `bbttcc:enlightenmentChanged`, `bbttcc:epic:*` ×3, `bbttcc:dialogue:secretDivulged`, `bbttcc:dialogue:courtDoorOpened`, `bbttcc:chase:resolved` (informational), `bbttcc:time:accrued` → campaign ledger (consumed — OK), `bbttcc:tour:started`.
**Ghosts** (consumed, never emitted): `bbttcc:raid:published`, `trigger.travel_threshold` family, `bbttcc:gm:advise` / `bbttcc:scene:enter` / `bbttcc:faction:sheetOpened` / `bbttcc:faction:opChanged` / `bbttcc:raid:initiate` / `bbttcc:manifestation:cast` / `bbttcc:courtly:scarAccrued` (mal-voice trigger declarations).
